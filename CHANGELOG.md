# 变更记录

版本号同步规则见 [`AGENTS.md`](AGENTS.md) 的「3. 改完的固定动作」。
1.0.2 之前的记录未留存。

---

## 未发布

### 守卫补齐：**三个测试数字**全部纳入自检核对（e2e / 集成 / 自检项数）

**纯开发期改动，不进包、对使用者没有任何影响。**

原先 `npm run check` 只核对**文档里写的 e2e 条数**（e2e 跑完会把实测条数写进
`.workbuddy/e2e-counts.json`）。另外两个数字**没人管**，而它们同样散在十几处文档里：

| 数字 | 原先 | 现在 |
| --- | --- | --- |
| e2e 断言总数 | ✅ 已守 | 照旧 |
| 集成测试条数 | ❌ 没人管 | 由 `integration-test.js` 落盘 `.workbuddy/itest-counts.json` |
| 自检项数 | ❌ 没人管 | 直接数 `check.js` 执行段里的 `checkXxx()` 调用 |

**为什么值得守**：1.2.7 把集成条数从 77 改到 106，是**手工改了 14 处**，全靠人眼；
本轮加了第 18 项自检，又要手工同步 11 处 —— **漏一处不会有任何提示**，
读者会以为测试套件比实际小（或大）。

**反向验证**（都先 grep 确认注入落盘）：
- 集成：`HANDOVER.md` 2 处改成 99 → 两条都报出
- 自检项数：`HANDOVER.md` 5 处改成 13 → 五条都报出
- 守卫自保护：把提取正则改到匹配不上 → 报「守卫失效」而**不是静默通过**

> ⚠️ **两个坑，都实测踩到了，值得记：**
>
> **① 数字常是加粗的**（`集成 **106** 项`）。判据若写成 `(\d+)\s*项`，
> "106" 后面跟着的是 `**` 而不是空白 → 正则**跳到行里下一个数字**去，
> 于是把 e2e 的 312 当成了集成条数（实测误报 3 处）。要写成 `(\d+)\**\s*项`。
> e2e 那段有同样的隐患，一并改了。
>
> **② 标志必须声明在用到它的代码块之前。** `itestChecked` 原先声明在集成校验块**后面**，
> 块里刚置的真被后面的 `var` 重置回 false —— 症状是**明明跑过 itest，
> 守卫却说「集成条数未校验」**（自己给自己发了一张假通行证）。
>
> **③ 一行里别混两个数字，而且判据要按"数字紧跟在关键词后"来写。**
> 自检项数**不能**沿用前两条的"关键词之后的第一个数字"—— 文档里有
> 「`npm test  # 自检 + 提示词基准 + 集成测试（…项）`」这种**没给自检报数**的行，
> 那样会把集成条数误当成自检项数。所以第三条改用**正向匹配**（`自检` 紧跟数字 + 项）。
> 同理，写说明时我把「与 e2e 同款」和「当前…项断言」放进了同一行，
> 于是 **e2e 守卫把那个集成条数当成了自己的** —— 两个守卫都只认"关键词之后的第一个数字 + 项"，
> **一行里只放一个数字**。

全量：自检 **18** 项 · 集成 **106** 项 · 真实 Edge e2e **29 个场景 / 312 项**，全绿。

## 1.2.7 — 修两处 `libs/` 里的静默错：API Key 清洗顺序、index 归一化

**这一版是 1.2.6 之后接着审 `libs/` 的结果**（前几轮审的是 `src/page/` 与 `content.js`）。
两处都符合这个项目最讨厌的那种形态：**不抛异常、不留日志，只是"行为悄悄不对"**。

### 修 `normalizeApiKey`：两个清理步骤的顺序反了

`libs/api-url.js` 的 `normalizeApiKey` 原先**先剥 `Bearer` 前缀、后剥引号**。
于是从 JSON 配置里整段复制出来的 `"Bearer sk-xxx"`（引号把 Bearer 一起包住）：

| 步骤 | 结果 |
| --- | --- |
| 先剥 `Bearer` | 它以引号开头，`^Bearer` 匹配不上 → 原样留着 |
| 再剥引号 | 变成 `Bearer sk-xxx` —— **前缀还在** |

而调用方是这么用的（`content.js`）：`'Authorization': 'Bearer ' + normalizeApiKey(key)`
→ 真正发出去的是 **`Bearer Bearer sk-xxx`** → 服务端一律 **401**。

**为什么这条特别值得修**：症状是"Key 明明是对的却报 401"，
而项目里现成的排查路径（`_markQuizApiConnectionFailed`、e2e 里那个"模型 401"场景）
会把人引向"Key 填错了 / 额度没了"—— **方向完全错**。而且 `"Bearer xxx"` 正是
从配置文件复制粘贴最常见的样子（文件头的注释里就写着这两种粘贴习惯）。

修法：**先剥引号、再剥 `Bearer`**，并用一个有界循环跑到稳定为止，
顺带覆盖双层包裹（`'Bearer "sk-xxx"'`、`"  Bearer sk-xxx  "`）。
`Bearer` 的匹配从 `\s+` 放宽到 `\b\s*`，好让"只粘了 `Bearer`"这种也能清干净；
同时保证**键名里本来就带 Bearer** 的（`Bearersk-abc` / `sk-Bearer-x`）不被误剥。

### 修 `normalizeItem`：`Number(null)` 是 0

`libs/prompt.js` 的 `normalizeItem` 开头写明：**`index` 为 null 表示"按位置对齐"**。
但实现是直接 `Number(rawIndex)` —— 而 **`Number(null)` 和 `Number('')` 都是 0**，
于是模型显式回了 `"i": null`（意思是"我没给序号"）时被当成 **index = 0**：
那条答案被硬塞给第 0 题，真正该拿它的那一题落空，
然后白跑一轮"空答案补问"—— **多花一次请求的 token**（这个项目最在意的成本项）。

修法：先把 `null` / `undefined` / `''` 挡掉，让它按声明落到 `index: null`。
⚠️ 注意 `i: 0` 是**货真价实的第 0 题**，不能和"没给"混为一谈（测试里有这条反向对照）。

### 新增集成测试 [11] [12]（共 29 条断言，集成总数 77 → 106）

两处都补了断言，并且**都做了反向验证**：

- [11] API Key 清洗：14 条输入 + 2 条"不能误剥"的反向对照 + 1 条"拼进 Authorization 头
  之后只有一个 Bearer"。注入旧实现 → **10 条变红**（含那条头部的，实际值就是
  `"Bearer Bearer sk-abc"`）
- [12] index 归一化：11 条 + 1 条多选数组完整性。注入旧实现 → **恰好 3 条变红**
  （`i:null` / `i:""` / `index:null`），而 `i:0` 那条**保持绿** ——
  证明断言分得清"没给序号"和"第 0 题"

> ⚠️ **反向验证时又栽在同一个坑上**：第一次用 `node -e` 内联多行字符串写注入脚本，
> shell 与 JS 的引号互相截断，脚本语法报错 → **什么都没注入** → 测试照样 106/106 全绿。
> 差点据此得出"断言抓不住"的错误结论。改成写一次性补丁脚本（带自校验：锚点没匹配就报错退出）
> 才做对。**注入后必须先 `node --check` + `grep` 确认落盘，再看断言结果。**

### 顺带确认没问题的两处（结论是"不改"）

- `libs/thinking.js` 的渠道识别与参数表：每档都显式标了 `verifiedLevels`，
  认不出的渠道默认一个参数都不发（最安全的默认），且"被 400 拒绝后摘掉参数重试"
  已有集成测试 [10] 覆盖
- `libs/api-url.js` 的三个地址构造函数：`/v1`、`/chat/completions` 各种写法都处理了，
  Gemini 的 `?key=` 拼接在已有 `key=` 时不会重复追加

全量：自检 **17** 项 · 集成 **106** 项 · 真实 Edge e2e **29 个场景 / 312 项**，全绿
（仓库根目录与打包产物各跑一遍）。

## 1.2.6 — 修诊断里写错的一个选择器大小写，并加守卫防复发

> 📌 **这一版把 1.2.4 / 1.2.5 / 1.2.6 三个版本一次带出来** —— 1.2.4 与 1.2.5 当时做完
> 没来得及发布（发版令牌被 revoke，推不出去），所以**从 v1.2.3 升上来会同时生效三块改动**。

### 修 `_describeQuizResultPage` 里容器计数写错的大小写（`.Cy_TITle` → `.Cy_TItle`）

**位置**：`_describeQuizResultPage`（`src/page/30-log.js`）—— 它专门回答
**「为什么没认出判分结果页」**。它输出的 `containers`（题目容器数）手抄了一份选择器，
里面写成了 `.Cy_TITle`，而真源 `_questionSelectors` 里是 `.Cy_TItle`。
CSS 类名**大小写敏感**，所以那个选择器**匹配不到任何元素，且不报错、不留日志**。

**影响：只影响日志，不影响行为。** 这份输出只被
`emitRuntimeLog('warn', 'quiz submit wait timeout', diag)`（`70-quiz-flow.js:699`）用来记日志，
**不参与任何判断**。

**但为什么仍然值得修**：这条日志**恰好在**用户遇到「提交后认不出结果页 → 25 秒超时」时才打出来，
而它存在的唯一目的就是告诉你**三道条件里缺的是哪一道**（这个函数的文件注释写得很清楚：
"不告诉你缺的是哪一道，于是 25 秒超时之后只能靠猜"）。
而**作业/考试页用的正是 `.Cy_TItle` 这一族**，于是 `containers` 会少算、在只用这一族的页面上直接报 0 ——
看日志的人会以为"页面上没有题目容器"，往错的方向查。
**诊断工具骗人，比没有诊断更糟。**

> ⚠️ **顺带更正我自己写错的结论。** 我最初以为受影响的是 `xxtAI.diagnose()` 的 `containers`。**错了。**
> `xxtAI.diagnose()`（`_diagnoseQuestionScan`）的 `containers` 走的是
> `_collectQuestionContainers()` → `_questionSelectors` 真源，**从来没被这个笔误影响过**
> （`30-log.js:222` 那行读的就是真源）。
>
> 真相是：**同一个文件里两个诊断各算各的** —— 一个走真源（对），一个手抄了一份（错）。
> 这正是"选择器清单只写一处、别在多个分支里各抄一份"这条规矩存在的理由：
> **手抄的那份必然漂移，而且漂移了不会有任何提示。**

**顺带发现一处"改了却没生效"**：这个修复当时只改在了片段
（`src/page/30-log.js`）里，**没有跑 `npm run concat`**，所以根目录 `page.js`
（浏览器真正加载的那一份）里**仍然是错的拼写** —— 也就是说修复一直没上线。
`npm run check` 的「page.js 与 src/page/ 一致」把它拦住了，而且值得记一笔：
**这个改动前后字节数完全一样（只差一个字母的大小写），靠比大小是发现不了的。**

### 新增自检第 17 项：选择器类名大小写一致

上面那个 bug 靠人眼发现不了（不抛异常、不留日志、页面上也看不出），所以把判据固化成守卫：
把代码里出现过的类名**按小写分组**，同组出现多种拼写即报错。

判据必须**收窄两处**，否则全是误报（都实测过）：

| 收窄 | 为什么 | 实例 |
| --- | --- | --- |
| 只看普通的 `.Foo` 选择器，不看 `[class*="Foo"]` | 第三方 UI 库类名大小写不确定时，本仓库既定写法是两套都列上 | `[class*="player"], [class*="Player"]`、`[class*="speed"], [class*="Speed"]`、`[class*="dialog"], [class*="Dialog"]` |
| 同组拼写出现在**同一行**的不算冲突 | 那是"并集写法"，两套都列了，不是笔误 | `.icon_Completed, .icon_completed` |

**反向验证过两个方向**：① 把 `.Cy_TITle` 改回 `.Cy_TITle` → 精准报出两个位置；
② 注入一行 `.Hedge_Probe, .Hedge_probe` 并集写法 → **不报**。
第二条同样重要 —— 守卫若因噪声频繁误报，下一个人就会把它关掉，等于没守。

> ⚠️ 顺带记一个**结论相反**的发现：`.icon_Completed` 与 `.icon_completed` 这一对
> **不是 bug**。`.icon_Completed` 是文档与本仓库公认的拼写（CHANGELOG / README /
> `_isDocumentFrameFinished` 都用它），`.icon_completed` 只在 `60-tasks-detect.js`
> 里作为**并集兜底**多列了一套 —— 并集只会匹配得更多，最坏情况是"多写的那套匹配不到"，
> 无害。**没有改它**，只在新守卫里放行。

### 把三处手抄的容器选择器合成一个真源 `_quizContainerSelector`

那个笔误之所以能发生，根子是**同一个选择器字符串被手抄在三处**：
`30-log.js` 的诊断、`70-quiz-flow.js` 的结果页判断、`72-quiz-answers.js` 的正确答案收集。
其中一处抄错，另两处是对的 —— 所以只改那一处只是"把这次的错补上"，
**下一次抄错还会发生**（这正是本仓库"选择器清单只写一处"那条规矩要防的事）。

现在三处统一读 `10-config-state.js` 里的 `_quizContainerSelector`。
**这是行为等价的改动** —— 三处原本就是同一个字符串，只是换成了引用，
选择器文本一个字没变，所以判定结果不可能变。

> 顺带说清它和 `_questionSelectors` 为什么**不能合并**：
> 后者是**有序的备选列表**（`_collectQuestionContainers` 逐个试、命中一个就用，还带嵌套去重），
> 目的是"尽量抠到题"；前者是**一次性并集字符串**，只回答"这一页有没有题目容器"。
> 形状和用途都不同，合了反而会把两种语义搅在一起。

### 自检第 15 项补上「状态字段清单」的核验（顺带查出一处已存在的漏同步）

核这一项时发现：头部注释里其实有**两份**清单 —— 方法清单（多行排布）和
状态字段清单（同一行排布），而守卫**只核了方法清单**。

于是 1.2.3 往 `10-config-state.js` 加了 `_quizRunPaperKey` 时没同步头部，
**头部数字静静地从 119 停在 118，没人发现** —— 又一次"错地图"。
现已把两种清单都核（数字 + 成员名逐个比），并修好那处漏同步（顺手补上本版新增的
`_quizContainerSelector`，现为 120 个）。

**反向验证**：往头部注入"删掉一个字段名 + 把数字改成 119" →
两条同时报出（`头部说状态字段有 119 个，实际 120 个` +
`头部漏了 1 个状态字段：_quizRunPaperKey`），且只报这一处。

### 给这个修复补了一条端到端断言

自检第 17 项能拦住"两个地方各写一种拼写"，但拦不住"**只有一处、写错了**"。
所以又在 e2e 的「抠题（作业考试页 Cy_* 结构）」场景里补了一条：
在**只用 `.Cy_TItle` 的作业页**上断言 `_describeQuizResultPage().containers > 0`。

> 为什么必须在这类页面上断言：`/quiz` 那套 mock 用的是 `.TiMu`，
> 而手抄的那份选择器里 `.TiMu` 拼写是对的 —— 所以在 `/quiz` 上断言**根本察觉不到这个笔误**。
> 断言必须落在"只用出错的那个类名"的页面上才有意义。

**反向验证**：把 `.Cy_TITle` 改回去 → **312 项里恰好这 1 条变红**，
失败信息是 `{"containers":0,"controls":14}` —— `controls` 有 14 个（页面明明有题目）
而 `containers` 是 0，正是"诊断骗人"的现场。其余 311 条全绿，
说明这条断言**只对应它该抓的那一个错**（`AGENTS.md` §5 要求的"一处注入只对应一条断言"）。

### 补上 background 里漏掉的一处超时（验证码图片抓取）

审计 `background.js`（115 行，此前从没单独审过）时发现：**两个紧挨着的网络函数，
一个加固了一个没加。**

| 函数 | 超时 |
| --- | --- |
| `apiFetch` | 有（`AbortController` + 30 秒） |
| `fetchImageAsDataUrl` | **没有** |

`fetchImageAsDataUrl` 是验证码图片的抓取路径
（`page.js` 的 `bridgeSend('fetch_image')` → `content.js` → 这个函数）。
图片服务器接了连接却不回数据时，这个 Promise 永远不 settle、`sendResponse` 永远不触发。

**影响说准**：调用侧的 `bridgeSend` 有 `BRIDGE_TIMEOUT_MS` = 90 秒兜底，
所以**用户不会真的永久卡住** —— 但那条桥接链路会一直挂着，
而且网络连接不会被释放（**abort 才能真正取消请求**，不是丢给系统回收）。

**修法**：照 `apiFetch` 的写法加 `AbortController`，超时取 **60 秒** ——
比调用侧的 90 秒早一步放弃（不让自己比调用方活得久），
又不至于把"慢但下得完"的小图（验证码图通常几十 KB）切断。

> ⚠️ **诚实说明：这个改动没有直接的自动化测试。** e2e 会走 `api_fetch` 那条路，
> 但**没有场景触发 `fetch_image_dataurl`**（要触发它得先有一个验证码图片的真实 URL）。
> 所以这一处只做了语法校验 + 全量回归，**行为本身没被断言覆盖**。

> 顺带记一个**结论是"不改"**的审计结果：`content.js` 与 `popup/popup.js` 里的
> `await fetch(...)` / `await response.text()` 看起来像"没超时"，**其实都有** ——
> 它们用的是 `AbortController` + `setTimeout(abort, timeout)`，
> 而且 abort 计时器到 `finally` 才清，**连读 body 都覆盖到了**。
> 自检第 16 项认不出这种写法（它只认同一行的 `_withTimeout(`），
> 所以这一条**只扫 `src/page/`**，不扩到全仓库 —— 扩了会一次报 13 个假阳性，
> 而**误报多了守卫就没人信了**。

### 新增能力：e2e 可以直接验**打包产物**（`OMITONE_EXT_DIR`）

发现一个**验证盲区**：`npm run e2e` 一直是**从仓库根目录**加载扩展的，
**不是用户下载到的那个包**。而 `tools/build.js` 用的是**白名单 `INCLUDE`** ——
万一漏登记一个文件，包在用户机器上跑不起来，**可 e2e 照样全绿**（因为它读的是根目录那份）。
发版前一直只靠"比文件哈希"间接确认包没缺东西，**从没验过"那个包真的能跑起来"**。

现在 e2e 支持换目录：

```bash
OMITONE_EXT_DIR=dist/omitone-1.2.6 npm run e2e
```

默认仍是仓库根目录（开发态行为一字不变）。注意扩展 ID 是**按绝对路径哈希**算的，
换目录会换 ID，`discoverExtensionId` 会在运行时找到真实 ID 并接管。

**实测结论：打包产物 `dist/omitone-1.2.6` 跑完 29 个场景 / 312 项，全绿。**
包内 `page.js` / `background.js` / `content.js` 与仓库根目录**逐字节一致**（sha256 比对）。

全量：自检 **17** 项 · 集成 77 项 · 真实 Edge e2e **29 个场景 / 312 项**，全绿
（**仓库根目录**与**打包产物**各跑一遍）。

## 1.2.5 — 音频也能拖到结尾（与视频一视同仁）

**背景**：用户反馈有些音频任务同样能「拖到最后」、平台照常计完成，希望音频与视频一样处理。

**原状**：`_trySeekToEnd` 第一行硬门槛 `tagName !== 'VIDEO'` 把音频挡掉（旧注释「只拖视频」），
`_isNinetyPercentVideo` 也硬编码只认 `video`。于是音频任务一律「从头正常播放」，不能拖。

**改动**：把这两处门槛从「仅视频」放开到「视频 / 音频」——

- 可拖动的音频直接 seek 到 `duration - 3s`（与视频**同一套逻辑、同一个 `enableSeek` 开关**）；
- 拖不动又锁 1x 的音频，也纳入「到 90% 且平台已标记完成 → 提前收尾」。

**为什么安全**：

- 拖不动的音频会被现成的「弹回检测」标记为不可拖，自动回落到正常播放，不与播放器对抗；
- 90% 提前收尾仍**必须由平台自己标记完成**（`_isDocumentFrameFinished`），拿不准时宁可多播、不误跳；
- PPT 逐页音频走的是 `_runPptAudioJob`（不设 `_videoEl`、不经这条路），完全不受影响。

- e2e 在 `/media` 场景加了两条断言（音频 seek 到 97、拖不动+锁1x 音频纳入 90%），并做了反向验证
  （分别注入还原两处门槛 → 对应断言精准变红）。
- 全量：自检 16 项 · 集成 77 项 · 真实 Edge e2e **29 个场景 / 311 项**，全绿。

---

## 1.2.4 — 讨论新开的窗口里插件不自动跑

**症状**：讨论任务点会 `window.open` 新开一个标签页，插件在那个新窗口里停在「关闭」状态、
一动不动 —— 讨论既没发言、也没返回继续刷课，整条讨论链路失灵。

**根因**：新标签页要不要自动跑，只看 `chrome.storage.local.xxtRunning`
（`content.js` 据此在 `<html>` 上设 `data-xxt-auto-start`，`page.js` 的 `shouldAutoStart()` 读它）。
而**页面正中央那个面板的【开始】只调 `app.run()`、从不写 `xxtRunning`**（只有扩展弹窗的【开始】才写）。
于是从页面面板开始刷课时 `xxtRunning` 一直是 `false`：讨论新标签页 `shouldAutoStart()` 为假 →
命中 `90-console-api-startup.js` 里「是讨论页且没开启 → 静默 return」→ 新窗口里插件不跑。

**修法**：把「已开启」标记落在 `run()` 里 —— 它是所有启动入口（页面面板 / 弹窗 / 刷新续跑）的
**汇合点**。`run()` 一执行就投递 `storage_set{xxtRunning:true}`，页面跳转、讨论 `window.open`
新开的标签页于是都能读到标记、`shouldAutoStart()` 为真、自动续跑。顺带修好了
「从页面面板开始后、跳一次页就不再自动跑」。

- e2e 新增场景「run() 落续跑标记」并做了反向验证（去掉这行 → 「落 xxtRunning」两条断言精准变红）
- 全量：自检 16 项 · 集成 77 项 · 真实 Edge e2e **29 个场景 / 309 项**，全绿。

---

## 1.2.3 — 一张卡片两份测验：第二份不做

**症状**：一张学习卡片里挂着**两份**测验任务点（实测：「专题五 邓小平理论单元测试」+
「7.4邓小平理论单元测试」），AI 做完第一份就过去了，**第二份连试都没试**。

诊断过程值得记一笔：前几轮我猜的方向（共用容器的完成标记、抽题跨文档兜底、
"整份文档级"的完成判据）**全部被实测数据否掉了**，最后是扩展自己的运行日志给出了答案。
日志里**没有任何一条"跳过"**，只有反复两轮这个组合：

```
study begin {"attachments":2}               ← 附件明明找到 2 个
study finished on page {"searchedCount":0}  ← 7 秒后宣布"本页没有任务点"，一个都没跑
study next unit                             ← 点「下一节」，整章被推过去
```

`searchedCount: 0` 是决定性线索：**它没有"跳过了第二个"，而是认为"这一页根本没有任务点"。**
（7 秒正好是 `waitTimeout = Math.min(3 + 附件数×2, 10)` 的搜索超时 —— 它只是等超时然后放弃。）

**根因是两个，各自都能单独造成"第二份不做"，缺一个都修不好。**

### 一、任务点身份（jobid）和题目（`.TiMu`）**不在同一层帧里**

真实结构是三层，而这两样东西**分在首尾两层**：

| 层 | URL | 有 jobid？ | 有 `.TiMu`？ |
| --- | --- | --- | --- |
| 外层 | `/ananas/modules/work/index.html` | ✅ `data` 属性里 | ❌ |
| 中间层 | `/mooc-ans/api/work?api=1&workId=…` | ❌ `data` 为空 | ❌ |
| 最内层 | `/mooc-ans/work/doHomeWorkNew?…oldWorkId=…` | ❌ `data` 为空 | ✅ 整卷题目 |

`_searchChaoxingJobOcs` 只认「**本帧自己的** `data` 里的 jobid」→ 带 jobid 的那层被
"没有 `.TiMu`"挡掉、有 `.TiMu` 的那层被"没有 jobid"挡掉 → **三层全被 `continue`**。

修法：新增 `_findJobIdInAncestorFrames(win)` —— 本层取不到就**沿祖先帧链往上找**
（jobid 挂在祖先帧的 `data` 上）。取不到仍返回空串，**不瞎猜**。

### 二、"已答过"是**页面级**的，同一张卡片里换卷子不会清

`_quizAnswered` / `_quizCurrentQuestions` 这一组只在 `_resetRuntimeState()` 里清，
而它的调用点只有：`run()` / 换章节 / `nextUnit()` / 换学习卡片 ——
**同一张卡片里从一份卷子切到另一份，一次都不经过**。
于是第一份交完后 `_quizAnswered` 恒为 true，`_handleQuiz` 第一行就直接 return。

修法：新增字段 `_quizRunPaperKey` + `_syncQuizPaperRunState(paperKey, paperDoc)`，
换卷子就把那一组状态重置。**三道保险**（每一道都有断言守着）：

1. 拿不到身份键（空串）→ **什么都不做**（宁可沿用，也不能因此把"已答"清掉重答一遍）
2. 键没变 → **什么都不做**（同一份卷子的重试/重做必须保留状态，否则会重复提交）
3. 换过去那份**本来就已完成** → 重新置回"已答"（不把交过的卷子当新卷子重答）

⚠️ 身份键**不能用帧的 `src`**：真实页面上两个任务点的外层帧 src **完全相同**
（身份只写在 `data` 里）。为此单列一个小方法 `_getQuizPaperKeyFromTask`，
并用一条断言把这个坑锁住。

### 附带：修掉 1.2.2 自己引入的 `tick progress` 日志洪水

1.2.2 加的心跳日志**每秒一条**（用户日志里刷了一整屏）。原因是 `_runTick` 开头把
`_tickProgressNote` 复位成 `''`，而 `_ensureOcsStudyRunner` 那个**脱离 tick 的异步循环**
每 1 秒调一次 `_tickProgress('ocs study loop')` —— 节流用的"note 变了"这个条件永远成立。
修法：**不再在 `_runTick` 里复位 note**，只保留 30 秒时间窗节流。

### 测试

- e2e 新增场景「一张卡片两份测验：第二份也要被匹配到并答」（16 条断言）——
  mock 页按**实测的真实三层结构**搭（外层带 jobid、中间层空、最内层有 `.TiMu`），
  且第一份内层带 `.ans-job-finished`，所以正确行为是"跳过第一份、挑中第二份"，
  不是"随便挑一个"。断言落在**匹配结果本身**（命中的是哪一层、它自己的 `data` 是否为空），
  不是只看日志。
- 全量：自检 16 项 · 集成 77 项 · 真实 Edge e2e **28 个场景 / 305 项**，全绿。
- **反向验证两次**（一处注入只对应它该红的那几条）：
  ① 去掉跨层查找 → `matched: null`，「三层嵌套下仍能匹配到任务点」变红；
  ② 让"换卷子"永不重置 → 「换到第二份卷子时'已答'被清掉」等 3 条变红。
  两次都先 grep 确认注入真的落盘（`page.js` 里看到标记）再看结果 ——
  否则会得到"假绿"。

> ⚠️ **这一版没在真实平台验证过。** e2e 用的是照真实结构搭的 mock 页；
> 用户需要在真实课程里再跑一次那张卡片，确认第二份被做掉。

### 附带更正（纯文档 + 一个审计工具）："状态字段都在一个文件里"这句过度承诺

拆分时在 `src/page/README.md` 里写了三条"好记的规则"，其中第一条是
**"所有非方法属性都在 `10-config-state.js`"**。这一轮把全量字段数了一遍，发现**它不成立**：

- `10-config-state.js` 的声明表里是 **119** 个属性
- 另有 **32 个字段是"用到才建"** 的，三种写法：
  `if (!this._seekTriedKeys) this._seekTriedKeys = Object.create(null);`（建的时候才建）/
  `if (this._onVideoEnded) el.removeEventListener(…)`（用前先确认）/
  `if (now - (this._blockedReloadAt || 0) > 180000)`（读取处兜底）

**这本身是刻意的写法，不是 bug** —— 惰性初始化不会被"某处忘了复位"弄坏，
比依赖声明初值更抗错。全量核对过：每个读取点都有兜底，**没有一处会读到 undefined 而崩**。
问题只在于**那句承诺把"能找到"说过头了**：照它去 `10-config-state.js` 找 `_rateEpoch`
是找不到的，而它是音源探测的代际号，很关键。

所以这一部分**只改文档**（`src/page/README.md` 与 `README.md` §8 的措辞改成
"声明式的非方法属性都在这里"，并把惰性初始化的约定写清楚），
外加新增 `tools/audit-fields.js` —— 按需跑，一次列出全量字段与"不在声明表里"的那些。

> ⚠️ 这个工具**只列不判**：它看不出 `this` 指谁，所以 `XHR.prototype.open` 里的
> `this.__omitoneUrl` 也会被列出来（那是 XHR 实例的属性，不是 app 的）。
> 全量核对时最后剩下的 3 个"没看到兜底"**全是这类误报**。

顺带核过一遍：17 个被置真的状态标志里，3 个"从不置假"的 ——
`_stepNavigationBound` / `_submitSnifferInstalled` / `_visibilityBound` ——
全是 `if (flag) return; flag = true;` 的**只绑一次**幂等守卫，属正常。


## 1.2.2 — 看门狗按「无进展」判卡死（原来会误杀正常的长任务）

**这一版修的是 1.2.1 没覆盖到的另一半。** 1.2.1 修的是"**挂起的** tick 醒来后会清掉新锁"，
而看门狗**判据本身**有问题：它判的是「tick 活了多久」，不是「多久没有进展」。

`_tickStartedAt` 只在 tick 开始时设一次、中途从不刷新（全库只有 4 处读写），
于是**只要 tick 跑满 150 秒就强制放锁** —— 而下面这些都是"正常干活但跑得久"的路径：

| 路径 | 位置 | 可达到时长 |
| --- | --- | --- |
| `_waitSlideAudioDone` 逐页等音频 | `40-media.js` | 单页上限 **600 秒** |
| `_runPptAudioJob` 外层逐页循环 | `62-tasks-run.js` | 页数 × 单页，无上界 |
| `_ensureOcsStudyRunner` 的 `while (version === …)` | `60-tasks-detect.js` | 开放条件，内含 `await job.func()` |

**后果比 1.2.1 修的那个更容易触发**：强行放锁 → 新一轮 tick 启动 →
重新扫任务点又选中同一个 → **两个 tick 同时操作同一个任务点**（重复翻页 / 重复提交）。
1.2.1 的 `_tickEpoch` 只堵住了"锁被误清"，**堵不住旧 tick 继续推进**。

### 修法：把判据从"总时长"换成"无进展"

新增进度心跳 `_tickProgress(note)`，由**长任务的循环**主动刷新；看门狗的判据改为
`now - (_tickProgressAt || _tickStartedAt) > 150000`：

- **有进展 → 不触发**（这是本次修正的行为，长任务不再被误杀）
- **真挂起 → 心跳停 → 150 秒后照旧触发**（防线一点没放松）
- 兜底用 `_tickStartedAt`：兼容心跳字段尚未初始化的边界

打心跳的 5 处：`_waitSlideAudioDone`、`_runPptAudioJob` 每翻一页、
`_ensureOcsStudyRunner` 的三个等待循环。日志带 30 秒去重，
顺带成了「这个长任务到底跑了多久」的观测点（用户下次跑课可从日志看到实际规模）。

`_tickEpoch` 与 `finally` 的复位逻辑**一个字没动**（1.2.1 刚修好，e2e 锁着）。

### 为什么之前没发现：这条路径在 e2e 里是零覆盖

`_runPptAudioJob` / `_waitSlideAudioDone` / 看门狗在 `browser-e2e.js` 里**一处都没测过**——
所以 280/280 全绿并不代表它是对的。本次新增场景「看门狗：按"无进展"而非"总时长"判卡死」
（6 条断言），并把 mock 缺口补上。

### 反向验证（两次注入，各自对应不同的断言）

- **注入「看门狗永不触发」** → 「真挂起时仍放锁」2 条变红；
  但「有进展时不放锁」**照样绿**——说明那条断言有另一层保护兜底
  （"永不触发"天然等于"不放锁"）。**这正是 `AGENTS.md` §5 警告的覆盖不全**，
  于是补了第 ③④ 条把它夹住。
- **注入「判据改回只看总时长」**（即修复前的行为）→ 3 条变红，
  报出的正是真实症状：`running:false, epoch:101` —— **正在推进的 tick 被强制放锁**。
  这是"旧判据确实会误杀"的直接证据。

## 1.2.1 — 修两处会让主循环卡住的 `await`，补上主循环的并发隐患

**这一版是 1.2.0 之后的代码审计结果**：把 `page.js` 按域拆开之后，
第一次能按域逐个审"这一域有没有会挂死的 await / 有没有并发的洞"，
于是查出了三处问题（两处卡顿、一处并发），外加补了两个从未声明的状态字段。
功能没变，AI 消耗也没变。

### 修看门狗"强制放锁"引入的并发隐患（可能两个 tick 并行）

`_startTickLoop` 里有个看门狗：`_runTick` 挂起超过 150 秒就**强制释放重入锁**，让循环恢复
（这条机制本身是对的，历史 bug 就靠它兜住）。

但它有个副作用：被强制放锁的那次 tick **并没有真的结束** —— 它只是还在挂起。
等它过一会儿醒过来，`finally` 会**无条件**执行 `this._tickRunning = false`，
把**新 tick 的锁**一起清掉；于是下一轮（250ms 后）又起一个 tick，**两个 tick 并行推进**
（可能重复提交、重复跳章）。

**修法**：给主循环加一个"代号"`_tickEpoch`，`_runTick` 在拿到锁时记下自己的代号，
`finally` 里**只有代号仍然等于当前值**才允许复位；看门狗强制放锁时先把代号 +1，
作废旧 tick 的复位权。

> 正常路径下代号必然相等 —— 所以**行为与改动前完全一致**，只有"挂起超过 150 秒"这条
> 病理路径变了。这也是这个改法风险低的原因：它不碰 `_runTick` 的判定顺序（那才是高风险的地方）。

### 补上两个从未声明的状态字段

`_tickRunning` / `_tickStartedAt` 一直**没有在 `10-config-state.js` 里声明过**，
全靠 `this._tickRunning = true` 现建。值上没坏（`undefined` 是假值，判断照常工作），
但它破坏了本项目"**状态字段全在 `10-config-state.js`**"这条规矩 ——
想找主循环的状态，在那个文件里 grep 不到，而这正是拆分要达到的效果。
现已补声明（连同新的 `_tickEpoch`），片段头部清单同步 112 → 115 个。

### 修两处会让主循环卡死的 `await`（无超时护栏）

**问题**：`page.js` 的 `_runTick` 是一条 `await` 链 —— 其中任何一处**永远不 resolve**，
整个调度就停摆（验证码检测、播放巡检、任务点推进全停），而且**页面一条异常都不会有**，
极难定位。这次审计发现两处裸 `fetch` 没有超时护栏：

> ⚠️ **先把影响说准**：看门狗（`_startTickLoop` 里那个"挂起超 150 秒就强制放锁"）
> 会把这种情况兜住，所以**不是"永久死亡"，而是"最长 150 秒整个插件不干活"** ——
> 期间验证码不处理、视频不巡检、章节不推进。修完降到 15~60 秒。

| 位置 | 调用链 | 危险点 |
| --- | --- | --- |
| `_maybeRepairMediaSource`（`40-media.js`） | `_runTick` → 播放失败 → 补救音源 | `await fetch(src)` + `await response.arrayBuffer()` |
| `_resolveDiscussionUrlFromModule`（`65-discussion.js`） | `_runTick` → `_tryDiscussionTask` → 解析讨论地址 | `await fetch(abs)` + `await resp.text()` |

服务器**接了连接却不回数据**时（CDN 卡住、被代理/门户吞掉），这两个 `await` 会一直挂着。
注意第二处外面本来就包着 `try/catch` —— **但 `try/catch` 拦不住"挂起"，只有超时能。**

**修法**：包一层已有的 `_withTimeout(promise, ms)`（fetch 头 15~20 秒、读 body 15~60 秒）。
`_withTimeout` 超时的行为是 `resolve(undefined)` 而不是 reject，
而这两处的既有判断 `if (!response || !response.ok)` / `if (!buf …)` / `typeof html !== 'string'`
**正好接得住 `undefined`** —— 所以改动只是"多一个上限"，正常路径一个字节都没变。

> 有意思的是：紧挨着这两处的 `video.play()` **本来就包了 `_withTimeout`** ——
> 说明作者知道这条纪律，只是漏了网络这一支。

### 新增自检第 16 项：`await` 必须可超时

上面那个 bug 靠人眼找不可靠，所以把判据固化成守卫（`AGENTS.md` §2 第 2 条本来就有这条规矩，
但一直没有机器检查）：扫描所有 `await`，只盯**已知会挂起的原生调用**
（`fetch` / 读响应体 `.text() .json() .arrayBuffer() .blob()` / 媒体 `.play() .pause()`），
要求同一行出现 `_withTimeout(`。

**刻意不写"名字里带 play/load 就算"那种宽泛规则** —— 实测会把 `_probeMaxPlaybackRate()`
（内部自带超时）误报成危险；误报多了守卫就没人信了，最后被人加白名单绕过去。

反向验证：往片段里插一行 `await fetch("/x")`，守卫立刻报
`30-log.js:103  fetch 没超时`；还原后全绿。当前 9 处已知会挂起的原生调用**全部**有护栏。

### 新增工具 `tools/audit-safedoc.js`（按需跑，不进 `npm test`）

`AGENTS.md` §2 第 1 条把"裸读跨域 `.document`"列为**最难查的一类 bug**，
但"有没有裸读"不能只靠 grep —— 决定危不危险的是**它有没有被 `try` 包住**。
这个工具做一次结构分析（按行算大括号深度、标记每行是否落在 `try/catch` 区间内）给出结论。

**本次审计结果：16 处裸读，全部在 `try` 块内，没有保护的 0 处。** 不需要改代码。

它没进 `npm test`，原因是它需要一个"判 `/` 是正则还是除号"的词法启发式 ——
第一版就栽在这里：`60-tasks-detect.js` 里一个含引号的正则字面量让它以为进了字符串，
从此整份文件后面的行全被剥空，那个文件里 4 处裸读**一处都没报出来**，而汇总行照样打印
"没有保护 0 处"。现在脚本自带自校验（原文命中数 vs 剥注释后命中数），
但一个启发式词法器不适合当"每次都跑"的硬门禁。改 iframe / 跨域相关代码时手动跑一次即可。

## 1.2.0 — 把 `page.js` 拆成 16 个按域片段，代码一行没改

**这一版只做一件事：让"改哪个域"变成"打开哪个文件"。** 没有任何功能变化，
AI 的 token 消耗也**一分没多**（证据见下）。

### `page.js` 拆分：切成 16 个按域片段 + 构建期拼接，行为不变

**做了什么**：`page.js`（9899 行 / 330 个方法）拆成 [`src/page/`](src/page/README.md) 下的
**16 个按域划分的片段**，新增 `tools/concat-page.js`（`npm run concat`）把它们按文件名排序拼回
**根目录的 `page.js`**。

**为什么这么做**：这份文件是当前最大的维护成本 —— 330 个方法**没有按域排列**
（每个域都横跨全文，"答题"类方法分布在 586–9860 行），而且**全文没有任何分段注释**，
改一个域要在近万行里翻找。目标不是"文件变小了"，而是**下一个人能在 30 秒内定位到他要改的那个域**。

**分三步走，先零风险、再有风险**：

**① 按物理行切分（只切不改）。** 验收标准是**拼接产物与拆分前逐字节相同**
（`cmp` 无输出，sha256 `fd20baa1…`）。这一步把"多文件 + 拼接"的机制建起来而完全不碰行为，
于是有了一条随时可回退的基线。顺带确认 `manifest.json` / `content.js` /
`web_accessible_resources` / `build.js` 的 `INCLUDE` **一处都不用改** ——
它们看的还是根目录那个 `page.js`，路径与文件名都没变。

**② 按域重组（12 个片段）。** 这一步方法换了位置、字节必然变，所以靠另外两条证据：

- **属性与行级的多重集比对**：重组前后 app 对象里 **434 个属性名完全一致**
  （无缺失、无重复），app 体里 **8644 行有效行一行不多一行不少** —— 证明只是换了位置，没搬丢东西
- **测试兜底**：`npm test` 全绿 + `npm run e2e` **26 个场景 / 280 项全绿**（真实 Edge）

**③ 把两块最重的再细分（12 → 16 个片段）。** `60-tasks.js`（2567 行）拆成
`60-tasks-detect`（识别与搜索）/ `62-tasks-run`（执行与等待，含文档任务点）/ `64-tasks-loop`（主循环、
放弃名单、学习卡片）；`70-quiz-flow.js`（2248 行）拆成 `70-quiz-flow`（整卷流程与提交监控）/
`72-quiz-answers`（答案缓存、规范化、候选与避开错答）/ `74-quiz-vision`（读图与预算闸门）。
判据同一套，比对对象换成细分前的片段：**492 个属性名完全一致**、
app 体**非空行 9055 行完全一致**，外加 `npm test` + `npm run e2e` 全绿。

**新增的两道守卫**（都是为了拦"改完立刻生效、下次拼接全丢"这种静默失效 ——
浏览器加载的是根目录那份产物，所以直接改它当下是通的）：

- `npm run concat:check` —— 产物与片段不一致就退出码 1
- `tools/check.js` 新增一项：调 `concat-page.js` 的 `buildPageText()` 比对产物与片段，
  **已并入 `npm test`**。`src/` 同时被排除在语法扫描之外（片段不是独立 JS，单独看必然语法错误）

两道守卫都做了**反向验证**：往片段里注入一行、确认 `--check` 真的报错退出码 1；
临时放一个缺头部结束标记的片段、确认直接报错。

**文档同步**：新增 [`src/page/README.md`](src/page/README.md)（模块地图：每个文件负责什么、
拼接机制、改片段的三条注意）；`README.md` 的第一屏「已知技术债」换成了新的拆分说明、
**§8 定位表改成"域 → 文件"一步到位**、§9/§10 补上 `npm run concat`；
`AGENTS.md` 与 `HANDOVER.md` 补上"`page.js` 是产物、改片段"这条硬规则。

**细分时踩到的两个坑**（都已修，留给下次）：

- **新旧片段重名时，清理旧文件必须发生在生成新文件之前** —— 否则"删掉被拆掉的源文件"
  会把刚写好的同名新片段一起删掉（脚本照常打印成功，只有比对时才看得出少了 43 个属性）
- **块间的注释必须跟着下面那个函数走**。夹在两个属性之间的 JSDoc 如果留在上一块尾部，
  重排后注释就和它描述的函数分了家（`_takeVisionBudget` 的注释差点留在 `70-quiz-flow.js`）

**副作用**：细分的 6 个文件里，块间空行从两个收敛成一个（其余 10 个仍是两个）——
纯排版差异，非空行一行没动。整体 `page.js` 因此少 150 行、约 300 字节。

**剩下的三个大文件**：`75-quiz-dom.js`（1365 行）、`40-media.js`（1095 行）、
`70-quiz-flow.js`（1042 行）—— 还能再分，但"一个域横跨上千行"的问题已经解决，收益明显变小。
要分就**一次只搬一个子域、搬完立刻跑 `npm run e2e`**。

### AI 消耗没有变多（本版专门核过）

拆分是**纯搬家**，所以"接入 AI 的人花的钱会不会变多"这件事可以从三个层面钉死：

1. **提示词与请求体一个字没动** —— 提示词在 `libs/prompt.js`、请求在 `content.js`，
   整个拆分期间 `git diff -- libs/ content.js background.js` **为空**。
   `page.js` 里根本不构造提示词，它只通过桥接发 `llm_request` / `llm_vision` / `llm_captcha`。
2. **`page.js` 逐行未变** —— 拆分前后 app 体的**非空行多重集完全一致（9055 行）**，
   即每一行代码都还在、只是换了位置。抽 `page.js` 里 5 个真正发起 AI/图片请求的方法
   （`_handleQuiz` / `_handlePopupQuiz` / `_handleCaptchaDialog` / `_describeQuestionImages` /
   `_recognizeCaptchaImage`）逐个比对，**代码行逐字节相同**。
3. **基准没动** —— `npm run bench` 输出与拆分前**完全一致**：单批 484 tokens、
   40 题整卷 2057 tokens（相对 baseline 省 44.9%）、前缀缓存命中率 99%、等价费用 52.2。
   拆分前后这两次跑出来的数字一模一样。

> 结论：**这一版不会让任何人的 AI 账单变化**。文件变多了，但喂给模型的东西没变。

### 新增自检第 15 项：片段头部守卫

拆完之后，"哪个域在哪个文件"完全靠每个片段头部那段注释传达 —— 它是模块地图的底稿。
头部长了短了、方法搬走了却没改头部，读的人就会照着错的地图找代码。
**有错地图比没地图更糟**：没地图时他会 grep，有错地图时他会信。

所以 `tools/check.js` 新增一项，把头部当断言守住：序号 `NN/MM` 必须与文件排序位置一致，
头部「本段的方法」清单必须与文件里真实的 `app` 方法一一对上
（`window.xxtAI` 上那 8 个手动入口不算 `app` 方法，判据里已排除）。

三种错法都做过**反向验证**：① 从头部删掉一个方法 → 报「头部漏了 1 个方法」；
② 往头部塞一个不存在的方法 → 报「写了 1 个本文件不存在的方法」；
③ 把 `30-log.js` 的序号改成 `09/15` → 报「头部序号是 09，按文件名排序应是 03」。

### 顺手修掉的两处

- `checkTestCounts` 原先写死找 CHANGELOG 的「## 未发布」段。这一版它被改名成 `## 1.2.0 — …`，
  守卫会**静默失去覆盖**（找不到就跳过）。改成取"第一个二级标题那一段"，发版改名不再影响它。
- `HANDOVER.md` §9 还挂着「`page.js` 体量（420KB / 9570 行）… **先补测试再动**」这条待办 ——
  拆分已经做完了，那条改成"继续细分（可选）"。

### 版本与文档

版本升到 **1.2.0**，六处同步：`manifest.json`（`version` / `name` / `default_title`）、
`package.json`、`popup/popup.html`（`<title>` + `.ver`）、`content.js` 状态面板品牌位、
`README.md` 徽章、`docs/manual.html`（说明书源文件，5 处版本戳）。
`HANDOVER.md` 的当前状态表、自检项数（15 项）、代码规模与扩展体积（约 882 KB）一并跟上。

发版前审查 `npm run audit:publish` 通过（无密钥 / 本机路径 / 邮箱 / 大文件误入公开仓库）；
`npm test` 全绿（自检 15 项 + 提示词基准 + 集成 77 项）；`npm run e2e` **26 场景 / 280 项全绿**。

### 为 `page.js` 拆分准备好执行说明（交给另一个 AI 做）

`page.js` 是一个 **9899 行的巨石**：一个 IIFE 里的一个大对象字面量、330 个方法、
**没有按域排列**（每个域都横跨全文，例如「答题」类方法分布在 586–9860 行）、
而且**全文没有任何分段注释**。改一个域要在近万行里翻找 —— 这是当前最大的维护成本。

本次**不执行拆分**，只把执行说明准备好（用户会把它交给另一个 AI 做）：

- 新增 [`docs/pagejs-拆分提示词.md`](docs/pagejs-拆分提示词.md) —— **自包含**，
  可直接整份复制给另一个 AI。内容包括：现状勘明（各域行数/跨度统计）、
  两阶段方案、九条硬性约束、验收清单、五条「假成功」陷阱
- `README.md`：交接区加了「已知技术债」入口（并更正过时的「9500+ 行 / 320 个方法」）、
  文档地图挂上提示词、§8 定位表补了**拆分后的目标域→文件映射**
- `HANDOVER.md`：新增 **§0.2.2 待办**，刷新 §0.2 快照

**方案的核心是分两阶段，第一阶段零行为风险**：
按**物理行区间**切分（只切不改）+ 构建期拼接，**验收标准是拼接产物与拆分前逐字节相同**
（`cmp` 无输出）。这样 `manifest.json` / `content.js` / `web_accessible_resources` /
`build.js` 的 `INCLUDE` 全都不用动 —— 它们看的还是根目录那个拼出来的 `page.js`。
阶段二才按域重组，**一次只搬一个域、搬完立刻跑 e2e**。

⚠️ 勘察时确认的三条关键机制（都写进提示词了）：
**e2e 从仓库根目录加载扩展**（不是 `dist/`）；`build.js` 用**白名单 `INCLUDE`**；
**`window.xxtAI` 是给用户的手动调试入口**，有些方法「看起来没人调用」其实是被手动调的，
**别当死代码删掉**。

### 乱选模式自审：一处逻辑错误 + 一处健壮性 + 两条缺失的测试

**① 逻辑错误（我上一轮判断反了）**：
`_rememberSubmittedQuizAnswers` 我原本**没有**加乱选守卫，理由是「怕不记已提交会导致重复提交」。
审计后发现**那是错的** —— `submittedById` 全代码**只有一个读取点**
（`_getLastSubmittedQuizAnswerForQuestion`），用途是「题目被判错、而答案域为空时，
回退用上次提交的答案来记录错误」。它**不驱动提交流程**。
所以乱选写的随机答案同样只会起污染作用（将来换 AI 会被当「错答案」避开）。已补守卫。

**② 健壮性**：`_buildRandomQuizAnswers` 里 `count > 8` 会被硬压成 4 —— 没必要，
而且选项真多于 8 时白白浪费了正确匹配的机会。改成只兜底「读不到选项」（0/1 个），
上限放宽到 26。

**③ 缺失的测试（最要紧的一条）**：前面所有断言都只验**答案形状**（数量、类型、非空），
**从没验过「乱选的答案真的填得进 DOM」**。而形状对不等于填得上 ——
填不进去 → 章节小测的提交前置（每题都有值）永远不满足 → 整卷永不提交 →
用户看到的是「什么都不发生」。已补端到端断言（逐题填、逐题查隐藏域）。

**④ 顺带修了弹窗提示的一处措辞**：原写「它和 AI 答题是二选一」，不准确 ——
实际上乱选**也要「AI 自动答题」那个开关开着**（关掉它 = 完全不答题）。

**⑤ 第二条缺失的测试（弹题那条路）**：主流程的乱选已端到端测过（逐题填 + 查隐藏域），
但**弹题是独立的一份代码** —— `_handlePopupQuiz` 里另有一个 `bridgeSend` 请求点。
只测主流程等于漏了半条链路。已补两条断言：乱选时弹题**不发任何请求**、且**真的被选上**
（不是空转 —— 弹题若选不上，站点会一直重弹）。

**验证**：e2e 276 → 279。反向验证三组：
去掉 3 处缓存守卫 → `{"reads":4}` 失败；
让乱选产出空答案 → 3 条失败，其中 `{"filled":1,"total":7}` 证明端到端那条真的在量填充；
去掉弹题那条乱选分支 → `多发了 1 次` 失败。

---

### 新增「乱选」模式：不接 AI，本地随机作答

给**不想配 API Key** 的用户用：只求把卷交出去，不求对。弹窗「答题」组里新增一个开关。

**接入点选得很省**：乱选产出一个**和 AI 同形状的 result**
（`{ success:true, data:[...] }`），于是下游的填充 / 提交 / 判分识别**一行都没改** ——
只换「答案从哪来」这一个源头。两处请求点（主流程 + 视频弹题）各加一个分支。

**与既有功能的冲突，逐条处理：**

| 既有机制 | 冲突 | 处理 |
| --- | --- | --- |
| `_getQuizApiUnavailableReason` 跳过整道题 | ✗ 会变成「什么都不答」 | 乱选时该函数直接返回空 |
| 正确答案缓存 / 错误答案缓存 | ✗ 随机答案会污染将来的 AI 模式 | 两个 `_remember*` 函数**直接短路** |
| 题目配图识别（视觉模型） | ✗ 答案都随机了，读图纯属白花 token | 乱选时跳过 |
| `enableQuiz` 开关 | ✗ 语义重叠 | 明确：**乱选也要 enableQuiz 开**；关掉它=完全不答题 |
| 提交次数上限 / 放弃机制 | ✓ 无冲突 | 保留（防死循环） |
| 讨论 / 评论任务 | ✓ 无关 | 不动 |
| `_rememberSubmittedQuizAnswers` | ✓ 那是**流程状态**不是答案质量 | 保留（去掉会重复提交） |

**验证**：e2e 268 → 276，八条断言（不再判定 API 不可用 / 答案数量对齐 / 形状合法 /
不碰答案缓存 / 关掉后恢复 AI 路径 /
**乱选真的随机**（60 次至少 2 种答案）/ **乱选优先于 AI**（配了 key 也走本地生成）/
**enableQuiz 关掉时乱选也不生效**）。
反向验证：三组注入各自命中 ——
去掉 3 处守卫 → 2 条失败（`{"reason":"api-key-missing"}` 与 `{"reads":2}`）；
让生成永远返回 A → `{"distinct":1,"keys":"A"}`；
让乱选要求「没 key」且忽略 enableQuiz → 另 2 条失败。

⚠️ 又一条**写完即改**的假断言：「乱选不写答案记忆（返回 0）」—— 测试环境里本来就没有
可记的错误答案，加不加守卫都返回 0，**分辨不了**。改成监听 `_loadQuizCorrectAnswerCache`，
断言「连读都不读」才抓得住。

### 多选补选不再覆盖「新鲜的」答案：只在已判错时才补

用户实测给了关键反证：**多选只选一个照样能提交成功**。
这说明「平台不接受单选多选」这个前提**不成立**。

回头看 1.1.5 自己的记录（成因 2），补选的真正目的是
「**让重试别总在只选一项里打转**」—— 那是个**分散重试**的启发式，不是平台约束。
但它在**第一次**就生效，等于**造一个模型没给的答案** —— 那正是判错的一个来源。

**改法**：只有当模型给的组合**已经试过且失败**（在错误缓存里）时才补选；
新鲜的答案原样使用。

**验证**：e2e 267 → 268，两条断言：
- 「新鲜的单个字母答案 → 原样使用（不凭空补第二项）」
- 「已判错的单项组合 → 才补成两项（让重试换组合）」
反向验证：恢复「总是补选」→ 两条立刻失败，报 `{"values":["B","C"],"hidden":"BC"}`。

⚠️ 过程中撞坏过一条**已有**断言（⑥「错够阈值前不放弃」）—— 因为我第一版的新测试
把 "B" 写进了**共用题** `window.__mq` 的错误缓存，污染了后面用同一道题的测试。
改成**用自己的合成题**（qid 99101）后互不干扰。
**教训：测试之间共用可变状态时，新增测试必须自带隔离。**

### 加只读抓包：把平台提交作业的请求体与响应打出来

「作业提交失败」查到这里卡住了：`btnBlueSubmit()` **没抛异常**（抛了我们会打印
`btnBlueSubmit failed`，日志里没有），说明拒绝发生在**服务端** —— 而服务端给的原因
就在那次请求的**响应体**里。所以加了一层只读抓包。

- 钩 `XMLHttpRequest.prototype.open/send`，只记 URL 命中 `work|submit|exam|homework|answer` 的请求
- 打印请求体与响应（各截断 800 字），日志前缀 `[Omitone] submit sniffer`
- ⚠️ **铁律：完全穿透。** 钩子自身出任何问题都必须原样走原方法 ——
  在这里抛一个错会废掉整页 AJAX，比原 bug 严重得多。每层 try/catch，且只读不改
  （不碰参数、不碰返回值）

**验证**：新增 1 条断言 —— 「装了抓包之后普通 XHR 仍能跑完」。
这条断言的第一版**是无效的**：探针 URL 用的 `/__sniffer_probe__` **不含关键词**，
钩子那条分支压根没跑到，反向验证也抓不住（注入的抛错根本不会触发）。
改成 `/__work_probe__`（会命中钩子）后，注入「不穿透」立刻失败：
`{"threw":"注入：钩子不穿透"}`。

### 「作业提交失败」排查：一次被实测推翻的假设 + 两条定位日志

用户实测给出了关键反证：

> 显示的是「**作业提交失败**」，但我**随便选了一两个**、甚至连多选都只选了一个，**可以提交成功**

**乱选都能提交成功 → 失败与答案对错无关**，是我们生成的提交状态平台不认。

这推翻了我上一版刚写的假设（「提交后表单被清空 = 平台判错重置 → 把答案记进错误缓存」）。
那个假设若留着，会把**正确答案误禁**，下一轮只会更错。已改成：

- **只记日志**（`quiz answers cleared after submit`），**绝不**据此把答案判错
- 但仍**结束等待**（`_quizSubmitPending=false`），不再白等满 25 秒 —— 这部分是有效的，
  反向验证：去掉这一支 → 断言立刻报 `{"held":true,"pending":true}`

**新增两条定位日志**：

- `submit payload` —— 提交前把平台**真正会提交的字段**（`#answer{qid}` + `#answertype{qid}`）
  整份打出来。把它和**手动成功提交时**的同名字段一比，就能看出差在哪（题型值？格式？空值？）
- `answer field written … ours=` —— 我们写进隐藏域的值。与提交前那条 `answer field` 一比，
  就能分辨「我们的值生效」还是「被平台覆盖」

**又一条写完即撤的假检查**：「提交后被清空 → 不把答案判错」。反向验证发现它不可能失败 ——
表单被清空时本来就**没有已填答案可记**，那次 `_rememberWrongQuizAnswers` 是空操作，
加不加它 `wrongs` 都是 0。留着就是假的检查，已删。

### 多选答案统一按字母序返回（消除「时灵时不灵」的一个来源）

现场（用户日志）：`clicked option ... letter= A / B / C / D`（按 A→B→C→D 点），
但 `answer field 405907427 value= DBAC` —— 隐藏域既不是点击顺序也不是字母序。
用户的原话是「**时灵时不灵**」：顺序碰巧对时能过，不对就判错重交。

查出来的不一致：本文件对多选答案的**规范形式**是排序过的
（`_canonicalQuizAnswer` 末尾有 `.sort()`），但真正决定**点选顺序**的
`_normalizeChoiceAnswerValues` 直接返回模型给的顺序 —— 模型回 ["D","B","A","C"] 时
就按原序点。现在统一成字母序。

⚠️ **这一条没能证明就是现场那个 DBAC 的成因**：`_clickOptionItem` 写隐藏域时
两条路本来就排过序（有徽标按 DOM 顺序累加、无徽标显式 `picked.sort()`），
而 DBAC 两种顺序都不是 —— 说明**有人在我们之后重写了它**，那是平台自己的 handler。
所以本轮同时加了一条 `answer field written ... ours=` 日志：
把它和提交前那条 `answer field` 一比，就能分辨「我们的值生效」还是「被平台覆盖」。

**验证**：新增 1 条断言（乱序输入 ["C","A"] → 返回 "AC"），反向验证：
去掉排序 → 立刻失败并报 `{"values":"CA"}`。
另有一条「乱序输入时隐藏域也是字母序」**写完即撤** —— 它永远不可能失败
（我们的写入路径本来就排序），留着就是一条假的检查。

### 提示词补「多选给全部正确项」—— 修模型对两个答案的多选题只给一个

现场（用户贴的 console 日志）：`fill question 1 type multiple answer A` —— 一道
**正确答案是两项**的多选题，模型只回了一个字母。

查下来是提示词的漏洞：SYSTEM 里只有格式样例 `m["A","C"]`，
**没有一句说「多选题要把正确项选全」**；而紧接着还写着「不确定也给最可能答案」——
这正好鼓励模型对多选题只给最可能的那个。
于是插件只能靠**本地补选**（`_expandMultiChoiceLetters`）猜第二个字母，
猜错（如真答案是 A+C、猜成 A+B）就会反复重交。

**改法**：格式行补 6 个字「多选给全部正确项」。

**代价**（`npm run bench` 实测）：system 70 → 78 token，同批重试输入 477 → 486，
前缀命中率仍是 99%，费用节省 89.4% → 89.3%。**多花 8 个 token 换掉一次本地猜测。**
题干/选项完整保留、normalizeItem 兼容性、集成测试 77/77、e2e 264/264 全过。

### 提交超时加诊断：说清「为什么没认出判分结果页」

现场报「作业页反复重交」，日志里只有一句 `waiting quiz submit result` 刷了 7 次 ——
**看不出缺的是哪一道条件**。`_isQuizResultPageFinished` 有三道（重做文案一票否决 /
判分痕迹 / 控件全部 disabled），任何一道不满足都只返回 false。

现在超时时把三道条件各自的结果、容器数、控件数与未禁用数、正文里命中的关键词
一起记进 `quiz submit wait timeout` 日志（`_describeQuizResultPage`）。
下次复现就能直接看出缺哪一块，不用再猜。

**验证**：新增 1 条断言（e2e 263 → 264）——「提交超时诊断能报出还有可交互控件 = 尚未判分」。
反向验证：让诊断永远返回空对象 → 断言立刻失败并报 `{}`，还原后恢复绿灯。

### 修「作业页反复重交」：多选补选不看禁选列表，反复补出同一个错答案

**现场**（用户贴的 console 日志）：同一批题反复填、反复提交，`waiting quiz submit result` 刷了 7 次。
日志里两处关键证据：`fill question 1 type multiple answer A` 之后却 `clicked option ... letter= A` **和** `letter= B`。

**根因**：模型对多选题只答了一个字母（如 `A`），1.1.5 起插件会在**本地**把它补成 `min` 项
（`_expandMultiChoiceLetters`，依据是「复选题两项答案远比一项常见」，纯本地、零 token）。
但这个补选是**确定性**的 —— 答 `A` 永远补成 `AB`。
而补选发生在 `_avoidKnownWrongAnswer`（避错机制）**之后**，于是：

```
答 A → 本地补成 AB → 判错 → AB 进禁选 → 重问模型 → 模型仍答 A → 又补成 AB → …
```

原注释写着「若仍被判错，`禁:` 机制会把这次的结果记下来换别的组合」—— 但**换不了**，
因为补选根本不看禁选列表。这属于「机制有了、线没接」的又一种形态。

**修法**：补选前先取该题的禁选集合（`_getKnownWrongCanonicalSet`），
补出的组合若已判错就换下一个候选；候选全被判错时不硬凑，退回模型原答案。

**验证**：新增 1 条断言（e2e 262 → 263）——「补选会避开已判错的组合」。
反向验证：删掉那行禁选检查 → 立刻失败并报 `{"join":"AB"}`（又补出已判错的组合），还原后恢复绿灯。

### 发版工具：补 `replace-asset`，核验改按 sha256

**起因**：v1.1.6 打 tag 之后，main 上又落了两个**会影响安装包**的修复
（`0f345a2` 4xx 退避、`1742b9f` 作业页回填），于是**线上那个 `omitone.zip` 是修复前构建的** ——
用户点下载拿到的是旧代码，而版本号和徽章看起来都是新的。这类问题以前只能靠人盯。

- **`verify` 的盲区**：它把「改动的文件 → 附件」只按 `ASSETS[].src` 映射，
  所以 `使用说明.pdf` 能对上（比字节数），而**打进 zip 的文件（page.js / content.js）
  永远找不到对应附件**，一律被判成「Release 上没有对应附件」→ 报红。
  讽刺的是它自己又建议「直接替换该附件即可」—— 自己给的建议自己认不出来
  （实测：用新命令换完 zip，verify 仍然报旧包）。
  现在两路分开映射：直接附件源比它自己，打进包的文件比**本地重新构建的 zip**。
- **比对改用 sha256 摘要**（GitHub 附件接口会给 `digest`），拿不到再退回比字节数 ——
  字节数相等不等于内容相等，能比摘要就别只比大小。
- **新增 `replace-asset <tag>`**：把校验器那句「删旧 assets → 重传同名」变成可执行的一步。
  附件名保持不变，所以 README 那条永久下载链接不受影响（AGENTS.md §7.5）。

**验证**：v1.1.6 已用新命令替换附件，`verify` 按 sha256 确认两个附件都与本地构建逐字节一致。
反向验证：往本地 zip 追加 1 字节 → 报「内容不一致：本地 0ea44bc6516b… ≠ 线上 31884ec40aa6…」并退出 1。

---

## 1.1.6 — 修「无任务点的微课文档卡住」，字体放大到能看清，视觉模型可选用（默认关）

> 这一版处理用户报的四件事：**① 有些微课 PDF/WPS 文档没有任务点时会卡住、毫无动作**；
> **② 弹窗和说明书上的字对普通用户太小**；**③ 视觉模型只用来认验证码，太浪费**；
> **④ GitHub 在线页面打不开 `使用说明.pdf`**（本版一并处理，见下面第四节）。
> 视觉那件事的排序原则由用户定：**能接受刷不了课，不能接受花了钱还是不行** ——
> 所以**先建成本闸门，再开功能**。

### 修「没有任务点的文档卡住不动」：三个漏洞

**漏洞 1：文档任务接管时不看有没有任务点。**
`_locateDocumentTask` 只要 DOM 长得像文档（有 `#panView` 或 `.pageNum`）就无条件接管，
于是主循环每轮都在这里 `return true`，**永远轮不到**最后那道
「纯文本页且没有任务点 → 直接下一节」的兜底。
页面表现就是「一动不动，日志也不再更新」。
→ 新增 `_frameHasTaskPoint()`：沿 `frameElement` 上溯 4 层找任务点痕迹
（`ans-job-*` 类名 / `jobid` 属性 / `data` 里的 `"job":true` / 文本含「任务点」）。
外层有任务点就接管；主文档有也接管；**两者都没有则不接管**，交回主循环走跳过。

**漏洞 2：附件调度路径漏掉了纯文档帧。**
`_searchChaoxingJobOcs` 的特征判定只覆盖 6 类
（视频 / 章节测验 / 图片阅读 / 带音频的 PPT / 超链接 / 限时阅读），
一个**只有 `#panView` 的纯文档 iframe** 不属于任何一类，
在 `continue` 那一行就被丢掉 —— 永远进不了调度。
→ 补第 7 类 `pagedDoc` 作为**兜底**（放在判定链最后，不抢上面更明确的类型），
并且它必须通过 `_frameHasTaskPoint` 才放行（页面上的说明性文档不该被当成任务）。

**漏洞 3：任务托管的文档读完之后不换章。**
`_handleDocumentTask` 在「已完成 + 由托管接管」的分支里**只清状态就返回**，
而托管入口 `_runChaoxingReadJob` 只在页面自己暴露出 `finishJob` 时才清 ——
大多数 PDF/WPS 文档页根本没有这个函数。
结果：文档确实读完了、完成标记也真的出现了，**却永久停在这一章**。
→ 清完状态后与非托管路径保持一致：自动连播开启时推进到下一节。

### 字体放大到普通用户能看清

弹窗宽度 360 → **400px**，基准字号 13 → **14px**，
**全库最小字号从 9px 提到 12px**（版本徽标、分组标签、按钮、提示、日志面板逐项上调）。
以前那些 9~11px 的标注在 1080p 屏上要贴着看，是「字太小」反馈的直接来源。

### 视觉模型从「只认验证码」放开到「也能看题」——默认关闭

先说结论：**默认关，因为图片贵。** 图片按视觉模型计费，通常是纯文本的十几到几十倍，
一道带图题可能有好几张 —— 替用户默认打开，就是替他默认花钱。

**先补的账（此前一行都没有）：** 视觉请求从不记录用量，
意味着最糟的组合是「用户在花钱、日志里一片安静、出问题查不出原因」。
新增统一记账（区分 文本 / 验证码 / 视觉 三类），
并解析 Claude、Gemini、OpenAI 三种协议各自的用量字段。
现在打开「查看日志」就能看到每次调用消耗了多少 token。

**图片刻意不进主答题链。** 主答题链的提示词是一段**每次都一样的长前缀**，
按前缀缓存计费约为原价 1/10；一旦把每次都不同的图片塞进这段前缀，
缓存会全部失效 —— 省下的看图钱会以十倍从输入侧漏出去。
所以图片走独立请求，只把结果**转成一段文字**（形如 `[配图: 描述]`）拼进题干。

**四道闸门（都在设置里可调）：**

| 闸门 | 默认 | 作用 |
| --- | --- | --- |
| 总开关 | **关** | 没明确开启，一张图都不发 |
| 单题张数 | 2 | 超过就不再看这题的图 |
| 单图体积 | 400 KB | 超限**直接跳过、不缩放**，不做无声的额外开销 |
| 每章次数 | 30 | **用完即停，并且一定在日志里留一条** |

「一定留一条」是有意的：**静默烧钱是最不能接受的失败方式。**
每章用满后会写一条警告，然后本章不再发任何看图请求，直到进入下一章。
会看图的模型要**单独填**（留空会退回主模型，而主模型多半看不懂图 —— 钱花了、事没办）。

### 说明书重写成「担架级」：每个动作拆成四问

上一版把说明书入口修好了（Pages 上的真网页），这一版解决**内容本身**的问题。
原来的说明书信息其实很全，但它默认读者懂「解压」「开发者模式」「API Key」——
而真正卡住的人，**卡点从来不是"不知道该装扩展"，是"不知道自己看到的那一屏算不算对"**。

所以所有需要动手的地方都换成了同一张表，四问：

| | 说的是什么 |
| --- | --- |
| ① 你会看到什么 | 动手之前屏幕上该长什么样 |
| ② 点哪里 | 具体点哪个字、哪个按钮 |
| ③ **怎样才算对** | 做对了会出现什么 —— **没有这一条，读者只能靠猜** |
| ④ 不对怎么办 | 这一步做不成时先查什么、去哪儿看 |

全文共 **12 处**这样的四问表，覆盖安装、配置、刷课、答题、验证码全流程。
另外新增：

- **第 0 章「五分钟装好」** —— 四步走完，只看这一章也能上手，
  每一步都写明"对不上就别往下走"
- **开篇两段零基础说明** —— "扩展装完是看不见的，这很正常"、
  "压缩包要解压，这是个高频坎"，都是新手第一次就懵的地方
- **第 7.1 节「日志里的英文都在说什么」** —— 把最容易看到的 8 行日志逐条翻译。
  以前日志是英文的、只在第 7 章表格里散着提，读者看到满屏英文会直接放弃
- 弹窗示意图按**真实的六个分组**重画（播放 / 任务点 / 任务点类型 / 答题 /
  讨论任务点 / AI 接入），并标出"哪些分组要 Key、哪些不用"

**配图仍是纯示意图，没有任何真实截图。** 这是刻意的：截图里会带上课程名、
姓名、学号，而这份文档是要公开给所有人看的。示意图能说明"位置和含义"就够了。

说明书从 14 页长到 **20 页**，PDF 仍是**全矢量**（0 位图，中文已嵌入），
1.05 MB → 1.35 MB。

**两条新守卫（都在 `tools/check.js` 的用户入口检查里）：**

1. **四问结构不许漏** —— 逐个检查每一张四问表里四个关键词是否齐全。
   只数总数是不够的（四问是四行，光看「怎样才算对」出现几次，漏一个发现不了）。
   这条守的是"页面照样渲染、肉眼看不出来"的那类退化
2. **README 写的说明书页数必须等于 PDF 实际页数** ——
   这个数字以前靠人记，结果长期漂移：先是写 13 页而 PDF 是 14 页，
   后来改成 14 页而 PDF 又变成 20 页。现在机器核对

3. **说明书自己也要能点着下载** —— 这条是补漏：README 有双入口，
   但说明书**正文里一个可点的下载链接都没有**，只写着"打开项目首页，点最上面那个按钮"。
   而说明书是会被**单独转发**的（有人把 PDF 或在线链接发给同学），
   对方手里只有这一份文件，那句话就成了空话。现在说明书封面直接给出两个直链
   （下插件 / 下这份 PDF），并由守卫盯着不许再删

⚠️ **第二条守卫写错过两次，都是反向验证抓出来的**（都值得记）：
第一次用 `使用说明\.pdf\*{0,2}\s*\|...`，但实际那格是
``**`使用说明.pdf`** | ...`` —— 反引号在后面挡着，正则匹配不上，**守卫静默通过**；
第二次改成"从文件名往后 40 字"，而「（20 页」的括号落在第 44 个字，
**照样静默通过**。最终改成先取整行、再在行内找「N 页」才真正生效。
**教训：守卫"永远通过"和"永远失败"一样危险 —— 不注入错误去试，就不知道它是不是死的。**

⚠️ **同一条教训在发布校验器上又出现了一次，且更隐蔽：**
`tools/github-release.js` 的 `verify` 改成读 `tools/build.js` 的 `INCLUDE` 白名单后，
读不到白名单的兜底分支只打印一句提醒，**然后把整段判断跳过了** ——
于是不管 tag 之后改了多少东西，都会掉进最后一个 `else` 打印
「tag 与 main 完全一致 ✓」。这不是"跳过"，这是**假装没问题**：
反向验证时注入一处必错的改动，它依然退出 0。
现在读不到白名单 = **校验器自己坏了 = 直接报红**，不再向下打印"一致"。

### 代价说明（不藏着）

- 视觉默认关闭，所以**升级后行为与 1.1.5 完全一致**，不会平白多花钱
- 开视觉后每道带图题的作答会**慢一拍**（要等一次看图请求），且这张图单独计费
- 无任务点文档的判据是「外层容器有没有任务点痕迹」。
  若某个平台把标记做得极其隐蔽，可能被误判成「无任务点」而跳过 ——
  这是有意的取舍：**宁可跳过，也不要卡住**（卡住用户完全不知道发生了什么）
- 说明书由 14 页增到 20 页，PDF 1.05 MB → 1.35 MB。
  重写只加文字与矢量图、**没有引入任何位图**，所以缩放印刷依然不糊
- 说明书封面新增两个下载直链（下插件 / 下这份 PDF）。
  这不改变正文内容，只是让**单独拿到这份文件的人**不用再自己去搜仓库

### 发版后补的三个修复（都在 tag 之后，不影响已下载的 zip）

**① 作业 / 考试页点了选项却不生效**（用户报：日志写着"已点"，页面上一个都没选中）。

那套 `.Cy_*` 结构里，选项**文本**在 `.Cy_ulTop` 的 `<a>` 里、可点的 `input` 在**另一个**
`.Cy_ulBottom` 里 —— 文本和控件是两个分开的 `ul`。
`_getOptionItems` 抢先命中文本那一列，`item.querySelector('input')` 恒为 null，
于是一个都没点到；但 `clicked++` 照样加，调用方当成功照样提交 → 空转。
→ 新增 `_pairOptionControls`：按索引把控件配到文本节点上；
点击顺序也改成**先点、再把终态写回**（反过来复选会被站点再切一次，反而变成未选）。
（参考 cxmooc-tools 的 `cxExamSelectQuestion`：它直接拿 input 当选项节点、文本另按位置取。）

**② 模型返回 4xx 被当成"网络连不上"，白等 45 秒。**

`content.js` 早就认得 401/403（不重试、不写 `apiConnectionFailed`），
但 `page.js` 拿回 `{permanentError:true}` 之后**根本没读这个字段**，
一律走 `_markQuizApiConnectionFailed` → 45 秒退避。
于是"Key 填错了"在用户眼里是"插件每隔 45 秒卡一下"，
而服务商明明把 `Invalid API key` 原话返回来了。
→ 4xx 单独一支：跳过本轮（60 秒）、把原话留在 `_quizApiLastError`，**不进退避窗口**；
视频弹题同理改走 `_giveUpPopupQuiz`（关掉弹窗 + 60 秒冷却，不拿坏 Key 反复撞接口）。

**③「题目在 `#frame_content` iframe 里」这条路径此前零覆盖。**

222/222 全绿并不代表它是对的 —— 补了端到端场景，并把 iframe 的 `src` 指向空页做反向验证。

### 发版后补的第四、五个修复（用户报「题目一直扫描，AI 重复提交」）

用户贴出的真实控制台日志非常典型：抠题正常、答案也填了、`btnBlueSubmit()` 也调了，
然后就停在 `waiting quiz submit result`，接着整页重载、重新扫描、重新答题、重新提交 ——
**无限循环**。逐层查下来是两个各管一段的洞。

**④ 作业/考试页点完选项，`#answer{qid}` 根本没被写。**

`_clickOptionItem` 写隐藏域那一段的条件是 `qid && badge` **同时**成立：

- `qid` 只从 `item.getAttribute('qid')` 取 —— **只认选项 `<li>` 自己身上的 qid**。
  真实作业页把 qid 挂在容器 `.Cy_TItle[qid]` 上，选项 `<li>` 是干净的。
- `badge` 要求 `.num_option / .num_option_dx` —— 真实作业页**根本没有这个徽标**
  （对照 cxmooc-tools：考试选择题的字母是从 `li` 的文本前缀推的）。

两个条件都不满足，于是**点击照做、日志照打 `clicked option`，隐藏域却一直是空串**。
`_getQuizQuestionFilledValue` → `''` → `_areQuizAnswersFilled` false →
`_quizReadyToSubmit` false → 整卷永不提交（或提交空表单）。
→ qid 改成**三级回退**（自身 → 最近的 `[qid]` 祖先 → `_getQuestionIdFromElement`）；
把"写隐藏域"从"徽标那一组操作"里**拆出来**，只要有 qid 就写；
字母改用 `_inferOptionLetter`，并且**以控件自身的 `value` 为权威**
（判断题的 `true`/`false` 就是这么来的 —— 写字母 `A` 进去平台不认）。

**⑤ 提交之后认不出「已完成」，于是无限重来。**

`_isQuizPassedOrFinished` 只认 `_isDocumentFrameFinished` 那一套标记
（`.ans-job-finished / .job-color / .icon_Completed / .testTit_status_complete`
+ 父层 wrapper 的「任务点已完成」）—— 那是**课程章节页**的标记。
作业/考试提交后翻出的是**判分结果页**（每题多出「我的答案 / 正确答案 / 解析」，
控件全部 `disabled`），这套标记一个都不命中 →
`_monitorQuizSubmit` 一直 hold → 25 秒超时 → 整页重载 → 重扫重答重交。
→ 新增 `_isQuizResultPageFinished`：三条判据**同时**成立才算完成
（出现判分痕迹 + 控件已不可交互或题目容器已消失 + 不含重做文案），
宁可漏判也不误判（误判 = 把没交的卷当已完成，直接跳过这个任务点）。

端到端相应补了两个场景（**26 个场景 / 280 项**）：
「作业页选项写入隐藏答案域」与「作业页提交后认出已完成」。
后者特意把 mock 的结果页做成**真实判分页**而不是一条「任务点已完成」横幅，
并断言**旧判据对它确实认不出来** —— 否则新判据是不是多余的，根本无从判断。

测试：自检 13 项 · 集成 **77** 项 · 真实 Edge e2e **280 项**（26 个场景），全绿。
---

## 1.1.5 — 修「多选题只选一个然后卡住」，加思考强度开关，铺开 AI 渠道

> 这一版处理用户报的三件事：**① 多选题有时只选中一个就提交、然后卡住**
> （章节小测不往下走、视频弹题反复选不对）；**② 想要一个思考强度开关**；
> **③ 除了 DeepSeek 还想用 Kimi / 通义**，并且要有地方记着这些渠道怎么核对。
> 全程守两条约束：**提示词一个字没加（token 不涨）**、**不把现有用户搞出 400**。

### 修「多选题只选一个然后卡住」：四个成因

**成因 1：选项填充逻辑有三份，其中一份缺了连写字母的拆分。**
章节小测走 `_fillMultiChoice`，它会把 `"AC"` 拆成 `["A","C"]`；
但视频弹题在 `_fillPopupAnswer` 里**另写了一份**，没有这层拆分。
模型只要回一个不带分隔符的 `"AC"`，`_matchOptionItem` 就一个都匹配不上 ——
这就是"扫描了、模型也答了，却从来不填"和"视频一直选不对"。

**成因 2：只选一项也算"填好了"，而重试又优先再选一项。**
"填了没填"只看**非空**，所以多选只勾 1 项照样提交 → 判错 → 记入错答 →
重试时 `preferredSize = canonical.length` 恰好是 **1**，于是 fallback 组合
**把"只选一项"的组合排在最前**，逐个字母 A/B/C/D 试过去，每一次都被判错，
一直磨到 20 次交卷上限。用户看到的就是"一直选不对、然后卡住"。

**成因 3：复选项被点了两次，而开两次等于没开。**
`_clickOptionItem` 先按当前状态取反写 class，再 `item.click()` + 内层徽标 click()。
单选无所谓，**复选是开关**，偶数次点击互相抵消 → 随机少选一项。

**成因 4：卡住之后没有任何机制让它"过去"。**
章节小测的提交前置条件是**每道题都有值**，所以一道题反复答不对，
整卷就永远交不出去 —— 这才是"一直卡住"的真正含义。

**修法：**

- 新增唯一入口 `_applyChoiceAnswer()`，章节小测与视频弹题**共用**，
  把原先三份重复的填充逻辑合成一份（净减代码）
- 连写字母按单字母拆开；答案去重（同一个字母点两次就是取消）
- 标着「多选」的题，答案不足 2 项时**本地扩成相邻两项** ——
  纯本地操作，**不额外发请求，零 token**
- `_sortMultiFallbackCombos` 的目标规模**下限钉在 2**；
  低于下限的组合**只排到最后、不删除** ——
  因为「不定项选择题」单选也是正确答案，删了会让那类题永远答不对
- `_clickOptionItem` 改成幂等：**先点击、再把终态写回**（class / aria / input.checked /
  隐藏域），复选只加不减；隐藏域写的是**全部已选项的并集**而不是最后点的那一个
- 多选填完后做一次校验 + 修补：只补"该选却没选中"的，绝不碰已选中的
- 同题错够 `quizQuestionMaxMisses`（默认 3）次后判为 best-effort：
  **不再为它发 LLM 请求**（省 token）、不再扩错答列表，直接填本地最优猜测，
  让整卷能交出去。有日志（`quiz filled best-effort guesses for repeatedly-wrong questions`）、
  可恢复（换卷即重置）
- 顺带修掉一处会让上面机制失效的旧行为：多选组合试尽时**不再清空**错答记录。
  清掉等于把"这卷试过哪些答案"整段忘掉，下一轮模型又会给出同一个错答案 —— 无限打转

**代价说明（不藏着）**：best-effort 的那道题会被填上一个**本地猜的**答案，可能仍然不对。
这是"跳过该题继续"换来的确定性：整卷交得出去，而不是被一道题钉死。

### 新增「思考强度」三档开关

弹窗里新增下拉框：**关闭（默认）/ 低 / 高**。

- **只有"关闭"是实测过的** —— 它就是本项目一直以来的行为，
  默认值也选它，所以老用户升级后行为**零变化**
- 「低」「高」的参数照各家官方文档写，**未在真实题库验证**，界面上如实标注
- 映射收进唯一真源 **`libs/thinking.js`**：DeepSeek 用 `thinking:{type}`、
  通义用 `enable_thinking`、Kimi 用 `reasoning_effort`
- **认不出的渠道在"关闭"档一个参数都不发** —— 各家对未知字段的反应不一致
  （有的忽略、有的直接 400），"不发"才是安全默认
- 真被拒收时 `content.js` **自动摘掉思考参数重试一次**并记日志，
  不让整次答题判死
- 弹窗里显示"当前实际发送：…"，把上面这些差别摊开给用户看 ——
  否则用户会以为"我关了思考它却没关"是 bug（Kimi K3 是恒思考模型，确实关不掉）

### 铺开 AI 渠道 + 新增渠道维护清单

预置从 4 个扩到 6 个：DeepSeek / **Kimi** / **通义千问** / Claude / Gemini / 自定义。
新增渠道的地址与模型名是照 **2026-09 的官方文档**填的快照，标签里明写「未实测」。

新增 **`docs/channels.md`**：各家 base_url、模型名、思考参数、**最后核对日期**，
以及"怎么用一条 curl 判断这家还活着"和"更新时要改哪几个文件"。
`HANDOVER.md` 里加了 §0.2.1，把"定期核对渠道"写成**固定待办** ——
厂商改版很勤（`deepseek-chat` 就已退役），写死的快照一定会过期，这件事不是可选项。

说明书（`docs/manual.html`）补了「其他服务商的密钥在哪申请」对照表与思考强度说明。

### 测试

- 集成测试 +15 条（新章节「思考强度：按渠道白名单发参数」，共 67 条）：
  逐渠道断言"该带的带对了"，以及**"不该带的一个都没有"**（后者更重要），
  外加一条失败路径：服务商 400 拒收时是否真的摘掉参数重试并拿到答案
- e2e 新增一个场景（17 条断言）：多选题的连写字母 / 单字母 / 重试下限 / 幂等点击 /
  不定项不被误伤 / best-effort 兜底），设置弹窗那一段加了渠道白名单、
  "未实测"标签、思考强度三档与落盘、说明文字联动的断言
- 为此给集成测试的 harness 加了可选的 `apiResponder` ——
  在此之前只能测成功路径，而现场出事的全是失败路径

---

### 修「`使用说明.pdf` 在 GitHub 上点开报 Unable to render code block」

**先纠正一个我最初也搞错的判断**：这不是"GitHub 不支持 PDF"（它支持），
也不是"文件太大超时"（1.02 MB 离 100 MB 上限差得远）。
真实成因是 README 里用的是 `[](使用说明.pdf)` **相对链接** ——
它在 GitHub 上会落到 `blob/main/使用说明.pdf`，也就是**代码视图**。
PDF 走这条路是**赌运气**：社区里该报错的头号成因是**浏览器扩展**
（暗色模式 / 翻译 / 广告拦截 —— 有人逐个关掉扩展才发现是暗色模式扩展），
其次是代理与渲染器约 5 秒的超时。用户实测点开就是报错，说明他那边赌输了。

**修法：把"在线查看"与"下载"分开，两条都不赌渲染器。**

| 入口 | 指向 | 为什么不赌 |
| --- | --- | --- |
| **在线看图解说明书** | `docs/manual.html` | 纯 HTML、零 `<script>`、零外部依赖；GitHub 渲染文本/源码是稳的 |
| **下载 PDF 版** | `releases/latest/download/Omitone-manual.pdf` | 走**下载通道**，与渲染器完全无关；附件名纯 ASCII |

`使用说明.pdf` **仍然留在仓库根目录**，但角色从"在线入口"降为"离线 / 转发"入口。
README 顶部与文档地图已按新角色改写。

**顺带修掉一个更隐蔽的问题：说明书源文件的版本号烂了。**
`docs/manual.html` 里同时躺着 `版本 1.1.1`（三处）与 `版本 1.1.5`（两处），
而当前是 1.1.6 —— 因为 `npm run manual` 的 `stamp()` **只盖临时副本**，
源文件不会被改回去。只要有人直接打开这份源文件（浏览器打开，
或将来开 GitHub Pages 当网页入口），看到的就是一份自己写着旧版本的说明书。
→ 已同步源文件的 5 处版本号（正文里「这是 1.1.0 专门修过的问题」这类历史引用**保留不动**）。

**`tools/check.js` 新增两条守卫，都做过反向验证（注入已知错误确认能抓住）：**

1. **双入口守卫** —— README 必须同时有 `docs/manual.html` 与
   `releases/latest/download/Omitone-manual.pdf` 两条链接，缺任一条失败。
   目的是拦住"将来有人图省事删掉在线入口、只留 PDF"这种**会静默复发**的改法。
2. **说明书源文件版本号守卫** —— 源文件里的 `版本 x.y.z` / `Omitone-x.y.z`
   必须等于当前版本；带历史措辞的引用跳过。

**发布流程这边还顺手做了两件事：**

- **令牌改放仓库外**：`~/.omitone-release.ghtoken`，
  并收紧 ACL 为仅当前账户可读写。理由见 `HANDOVER.md` §0.5 ——
  仓库内的文件哪怕 gitignore 挡住了，也随时可能被 `git add -f`、被别的工具打包。
- **记录一个新坑**：本机让令牌经**环境变量**传入连续 3 次都是空值，
  而写进文件立刻可用。别去查原因，直接走文件（`HANDOVER.md` §7.9）。

## 1.1.4 — 修「弹题答错后卡住」，并把设置弹窗重做

> 这一版处理接手时用户报的两件事：**① 视频里的弹题答完、答案不对就直接卡住，
> 等几秒也没后续**；**② 设置弹窗"太潦草"**。
> 另外复核了上一版留下的「三处媒体收尾逻辑分叉」——确认统一是对的，**没有改**。

### 修「弹题答错后卡住」：两个成因叠在一起

上一版（1.1.3）修的是**另一条**根因：去重指纹会随平台的「回答错误」提示文本变化，
导致"最多问 3 次就放手"的安全阀永远触发不了。那条改对了，但**症状没完全消失**，
因为还剩两处：

**成因 A：填完答案后设了两个窗口，长的那个把短的盖住了。**
`_handlePopupQuiz` 填完后同时设了「静默期 8 秒」与「已答放行 **30 秒**」，
而 `_activePopupBlock()` 在**后者**范围内一直返回 null。于是答错之后有整整 **22 秒**
处于"静默期已过、却仍被当成已答"的**没人管空档** —— 弹窗挂在那儿，
既没人重试，也没人关掉它。用户说"等 5 秒也没后续"，等的就落在这 22 秒里。
现在两个窗口共用同一个常量 `POPUP_QUIZ_QUIET_MS`：死等从 30 秒降到 8 秒，
而且**静默期一过就真的会重试**。

**成因 B：恢复播放的守卫用错了判断。**
`_handleVideoPause` 原先也用 `_activePopupBlock()` 判断"有没有弹窗"。
可静默期里它返回 null —— 那是给"要不要再问模型"用的答案，**不代表弹窗已经没了**。
而站点正是为了弹题才**有意暂停**视频；据此去抢恢复播放，就是和站点对打，
现场表现同样是"答完之后视频不动、看着像卡死"。

新增 `_popupQuizBlocksPlayback()`：**静默期也算挡着**，专供恢复播放那条路。
"该不该处理这道题"和"能不能恢复播放"本来就是两个问题，以前共用一个函数才出的事。
放弃窗口内它返回 false —— 那道题已经决定不管了，就不该再拦着播放。

顺带让恢复过程变得可见：答案被平台拒时会留一条
`popup quiz answer rejected by site, retrying with a different one`，
`popup quiz answered` 日志也带上 `waitMs`。
用户原话是"不确定是不是过一段时间就能修好"—— 说明失败仍然是**无声**的。

**三条新断言 + 反向验证**：把 30 秒改回去，断言立刻失败，且是 196 项里**唯一**失败的一项，
证据是 `afterQuiet.active: false`（弹窗确实无法重新处理）。

### 复核「三处媒体收尾逻辑分叉」：上一版的统一是对的

`_handleVideoEnded` 改走 `_finishCurrentMedia`、不再多清 `_activeDocumentJob*`。
逐条验证它给的三条依据，**全部成立**：

1. `nextUnit()` 末尾会调 `_resetRuntimeState()`，那几个字段本来就会被清 —— 正常路径下多清是纯冗余
2. `nextUnit()` 在 `autoNext:false` 时会**提前返回**，此时清掉等于
   **放弃一个可能正在进行的文档任务点**（本仓库最怕的"静默漏做"）
3. 文档任务点完成时只清自己，且有 120 秒 `document stuck timeout` 兜底 —— 媒体/文档对称

e2e 里有一条专门构造该场景的断言锁着。**结论：不需要改。**

### 设置弹窗重做

原来是一整列约 30 个控件从头排到尾：「开始运行」埋在最底部、要滚很久才点得到；
「AI 接入」那一大块（4 个输入框 + 2 个下拉 + 1 个文本域）对**不接 API 的人**完全是噪音；
主按钮和开关全用近黑色，看着像半成品。

- **主操作上移并吸顶**：顶部是状态（圆点 + 「已开启 / 未开启」）＋「开始运行 / 停止」，
  再加一句"刷课不需要 API Key"，把第一次用的人直接引到对的地方
- **分组折叠**：用原生 `<details>/<summary>`（零 JS、零依赖，且天然支持键盘）。
  默认只展开「播放」，其余收起
- **次要操作收拢**：检测连接 / 查看日志 / 清空日志 / 恢复默认 变成一行小号文字按钮；
  「保存 API」移进「AI 接入」组内（它本来在那儿才合理）
- **配色**：主色改成 `#1f6feb`，与 README 徽章、图文说明书同一套；
  成功用绿、警告用橙、错误用红接进提示条
- **顺带修掉中英混排**：`Testing...` / `Test API` / `Please enter API Key` /
  `Please enter model` 这几处英文串在中文界面里很突兀，已全部改成中文
- 体积：`popup.html` +3.5 KB、`popup.js` +0.2 KB。
  扩展整体 **784.2 KB / zip 326.1 KB**（整体增长主要来自 1.1.3 的 `page.js`：350 → 373 KB）

### 测试

自检 13 项、集成 52 项、真实 Edge e2e **196 项**（19 个场景），全绿。
顺带一提：「断言总数守卫」这次真的发挥了作用 —— 我加了 3 条断言后它立刻失败，
点名 6 处文档还写着 193，逐一同步后才通过。

---

## 1.1.3 — 2026-09-17

这一版主要处理两件事：**省掉不必要的时间**，以及**让失败不再无声**。

- **防拖拽 + 锁 1 倍速的视频**：平台只要求看满 90%，现在播到 90% 且平台标记完成后就进下一个，
  不再白等最后 10%。判据很严：拖不动 **且** 倍速锁在 1x **且** 平台自己说完成了，缺一不可
- **修掉「视频内弹题答错后卡死」**：根因是去重指纹会随平台的「回答错误」提示一起变化，
  于是「最多问 3 次就放手」的安全阀**永远触发不了** —— 会一直重问模型，而且**不会自愈**。
  上一版我以为「等 60 秒会恢复」，那是错的
- **弹窗界面重做**：统一视觉规范，并补上**无障碍** —— 10 个开关原先键盘完全用不了
  （`role` / `tabindex` / `aria-` 一个都没有），现在可 Tab 聚焦、空格切换、读屏能播报状态
- **三个新守卫**（`npm run check` 11 → 13 项）：配置项「读了必须有默认值」、文档场景数、版本号五处。
  其中「测试数字守卫」后来又扩了一半：**断言总数**也纳入校验了 ——
  这个数字**静态数不出来**（含每场景动态断言），所以由 `npm run e2e` 跑完把真实值落到
  `.workbuddy/e2e-counts.json`，`check.js` 再拿它比对文档。没跑过 e2e 就优雅跳过，
  不误判为失败。（起因：这个数字我已经连续两次忘记同步。）
  前两个一上线就各抓出真问题；第 12 项后来又扩了一半，见下条
- **「配置接线守卫」再扩一半**：不只查默认值，还查**界面上每个开关是否完整接线**
  （bindToggle 绑定 / load 里读回 / 保存时写出，缺一件就是「看着能点、其实不生效」或「存不住」）。
  ⚠️ 用**变量名**做纽带而不是假设「UI 的 id 就是配置键」—— 实测 `autoMaxRate` 的配置键
  其实叫 `autoMaxPlaybackRate`，按 id 猜会误报（我第一次就误报了）
- 图标 128px **无损**瘦身 28.6 → 21.9 KB（该图 100% 不透明，alpha 通道纯属冗余）
- **弹题答错后不再死磕同一个答案**：`previousWrongAnswers` 这个机制早就有了
  （`libs/prompt.js` 会渲染成「禁:1=D;」告诉模型别再选），普通答题一直在传，
  **弹题这条路从来没传过** —— 于是每轮重试都是逐字节相同的请求，模型每轮都回同一个答案。
  现在接上了；并补一条诊断日志记录模型原始答案，便于分辨「模型答错」还是「匹配错」
- **弹窗加「自动运行」状态指示**：顶部的圆点以前**纯装饰**（`popup.js` 里出现 0 次），
  打开弹窗完全看不出是不是已开启。现在圆点 + 文字徽章会跟着 `xxtRunning` 变 ——
  而且 e2e 断言特意查它**跟着状态变**，不只是查「有没有这个元素」，免得它退化成摆设。
  ⚠️ 措辞是「**已开启 / 未开启**」而不是「运行中」：`xxtRunning` 是个**持久标记**，
  它驱动的是「页面刷新后自动恢复」（见 `content.js` 的 `maybeMarkAutoResume`）。
  用户开启后切到别的网站，扩展并没有在跑，但标记仍是 true —— 写「运行中」就是骗人

### 防拖拽 + 锁 1 倍速的视频：看到 90% 就进下一个

有些任务点的视频被老师设成**防拖拽 + 锁 1 倍速**，平台的要求是"观看时长 ≥ 总时长的 90%"。
这类视频拖不动、也加不了速，播过 90% 之后那最后 10% 纯粹是白等。

判据是**两个"平台不让我们加速"的信号同时成立**，只满足一个都不算：
拖到结尾被播放器弹回（不可拖拽）、倍速探测结果就是 1x。

**最关键的安全阀：必须由平台自己给出"任务点已完成"的标记才提前结束。**
这里**没有**用"播够 90% 就当作完成" —— 那会在平台还没认可时**误跳过任务点**，
比多花十分钟严重得多，也与本仓库"拿不准一律当作完成"的一贯取舍一致。

- 新增配置 `advanceAtNinetyPercent`（默认 `true`）。前端开关由另一个 AI 接，
  键名就是这个；`popup.js` 的保存是合并式的，不会把它丢掉
- `_trySeekToEnd` 现在会记录"拖拽有没有被弹回"，这个结论被上述判据复用
- 顺手把"视频播完"的收尾逻辑抽成 `_finishCurrentMedia(reason)`，给 `ended` 与 90% 两条路共用
- 新增 e2e 场景 `/media-90`（19 项断言）。**两条反向验证都做过**：
  去掉平台完成标记检查 → 安全阀断言失败；收尾漏清 `_videoEl` → 收尾断言失败。
  其中安全阀那条第一次**反向验证没抓住**（断言里进度只有 50%，被"不到 90%"先挡掉了，
  根本没测到完成标记），已把进度改成 95% 并去掉标记，重验才抓住

### 新增「配置项默认值」守卫（`npm run check` 第 12 项）

三份默认值（`page.js` 的 `DEFAULT_CONFIG` / `content.js` 的 `configs` / `popup.js` 的 `DEFAULTS`）
服务的目的本来就不同，**不该长得一样**，所以"三份键不一致"本身不是 bug。
真正会出事的是另一种：**某个文件读了 `configs.X`，但它自己的默认值里没有 X** ——
读到的永远是 undefined，开关表现成"打开了也没用"，而且不报错、不留日志。

这条守卫只查这一件事，一上线就抓出两个漏网的：`popupQuizMaxAttempts`（弹题最多问几次，
以前只靠 `|| 3` 兜底，默认值表里根本查不到）与 `apiConnectionError`（诊断用，缺了不影响功能）。
两个都已补进 `page.js`。**已反向验证**：删掉任一默认值，自检立刻失败并点名。

### 版本号检查扩到五处（含 package.json 与 README 徽章）

`check.js` 原先只比对 manifest / popup / content 三处，而 `package.json` 与 README 顶部的
「版本」徽章**都不在检查范围内** —— 它们漂了不会有任何提示。README 那个徽章尤其要紧：
它是用户第一眼看到的版本号。

现在两处都纳入「版本号一致」这一项，并做过**反向验证**：把徽章改成 1.1.1、package.json 改成 1.0.9，
自检立刻失败并分别点名；还原后恢复通过。

顺带修掉本文件开头一个**指向不存在锚点**的链接（`#24-改完必须同步版本号三处`，
AGENTS.md 里根本没有这个标题），改为指向真实章节。

### 新增「场景数」守卫（`npm run check` 第 13 项），并清完 §9 剩余项

**场景数守卫**：`19 个场景` 这个数字散在五份文档里，全靠手工同步，历史上已漂过三轮
（14 → 18 → 19）。场景数是**静态可数**的（`SCENARIOS.push` 有几个就是几个），所以能真正守住。
断言总数是运行期才算出来的，静态守不住，不查。
⚠️ CHANGELOG 里历史版本写下的场景数（14、18）都是**当时的事实**，所以只查它的「未发布」段 ——
（这段说明本身也不能写成那个格式，否则会被自己的守卫抓 —— 实测抓过一次）
这条特意做了反向验证：历史记录在，自检照样通过。

**§9 剩余项清完**：
- 验证码提示：弹窗里原来写「留空用主模型」，读起来像能用 —— 改成明确的「必须填一个能看图的模型」，
  并说明留空的后果（识别不出来 / 可能识错 / 错了白跑一轮）
- 不支持连线、排序、拖拽题：README 的已知限制里**原本一个字都没写**，已补
- 图标 128px：从 28.6 KB 无损压到 **21.9 KB**。§9 原来写「可无损压到 ~10KB」是**错的** ——
  该图有 5618 种颜色（>256），转调色板会有损；但实测它 **100% 不透明**，alpha 通道纯属冗余，
  所以丢掉 alpha 转 RGB 是无损的，已逐像素比对确认（差异像素 = 0）
- `configs` 那条改写：三份默认值本就不该一样，真正的风险已由第 12 项守卫覆盖，剩下不做

测试：自检 13 项、集成 52 项、真实 Edge e2e **193 项**（19 个场景），全绿。

> 已解决：三份「managed media job ended」收尾已经**统一** —— `_handleVideoEnded` 现在走
> `_finishCurrentMedia`。它原本多清 `_activeDocumentJob*`，那三行是**过界的**
> （`nextUnit()` 提前返回时会放弃正在进行的文档任务点）。e2e 有断言锁着，别再拆回去。

---

## 1.1.2 — 把「下载」这件事从开发者视角改成使用者视角

> **扩展代码与 1.1.1 完全一致**，只改了版本号字符串。这一版动的是文档与分发，
> 目的是让**不会用 GitHub 的人也能装上**。
>
> 起因很直接：README 原先教的是"下载本仓库 → 解压 → 加载"，
> 而"本仓库到底怎么下载"用的是面向开发者的说法 —— 按钮在哪、要不要注册、
> 要不要装 Git，一句没说。真正会卡住人的恰恰是这些。

### 说明书搬出 `docs/`，并从"手工维护"变成"一条命令生成"

- **`使用说明.pdf` 挪到仓库根目录**（原 `docs/Omitone-manual.pdf`）。
  它是给"完全不懂 GitHub 的同学"看的，放在 `docs/` 里他们根本找不到。
  `docs/manual.html` 仍是排版真源
- **新增 `tools/manual-pdf.js`（`npm run manual`）**：从 html 生成 PDF 到根目录，
  顺带治好了**手册版本号漂移** —— 以前版本号要手工同步，而 1.1.1 发布时手册里还写着旧号；
  现在由脚本从 `manifest.json` 盖入，人没有机会漏
- 脚本自带产物校验：**位图数必须为 0**（说明图都是矢量，缩放印刷不糊）、页数不能异常少。
  无头打印的 stdout 没有任何有用信息，"看产物"才是唯一的成功判据

### README 顶部加「下载即用」+「三步装好」

- 第一屏直接给出**永久下载直链** `releases/latest/download/omitone.zip`。
  这是 GitHub 按**附件名**取最新一版附件的链接 —— 点一下就开始下载，
  不需要注册、登录、点绿色的 Code 按钮、装 Git
- 新增「三步装好」：下载 → 解压 → 加载，按"从没用过 GitHub"来写，
  并把**最大的坑单独标出来**：**必须「全部解压缩」，不能直接选 zip** ——
  这是非技术用户最常栽的地方
- 说明书链接改指根目录那份，并说明"可以直接转发给同学"
- 开发命令里的 `dist/omitone-1.1.1/` 改成 `dist/omitone-<版本>/`，以后升版本不用再改文档

### 发布附件名固定为 `omitone.zip`

README 的下载直链按**附件名**取件，附件名一带上版本号，**每发一版这条链接就失效一次**。
而失效的表现是"新用户点下载看到 404"——不报错、不留日志，
受影响又恰好是那批"只会点这一个链接、也不会来反馈"的用户。

- 新增 **`tools/github-release.js`**（`npm run release -- …`）：
  `push` / `release` / `verify` 三个子命令，走 REST API 完成提交、打 tag、建 Release、
  传附件、核验。**附件名在代码里写死**，不给人改错的机会
- `verify` 会确认三件事：tag 指向 HEAD、附件 `state=uploaded`、
  以及 README 那条下载直链确实有同名附件 —— **接口返 200 不等于传完了**

### 新增「用户入口守卫」（`npm run check` 第 11 项）

守住的是**非技术用户唯一的入手路径**：

- 根目录必须有 `使用说明.pdf`，且不能同时留着 `docs/Omitone-manual.pdf`（避免两份各自漂移）
- README 必须含**不带版本号**的下载直链
- README 里所有相对链接指向的本地文件/目录必须真实存在（点开 404 最容易悄悄发生在这里）
- 说明书源文件不许写死带版本号的压缩包名

四项都做过**反向验证**：临时制造 5 个已知错误，逐一确认能被抓住，再还原。
（不会失败的检查等于没有检查 —— 这是本仓库自己的规矩）

### 交接文档扩成完整接手稿（`HANDOVER.md`）

上一版只是一份"接手索引"，够指路但不够开工。这一版扩成**能直接交给下一个 AI 独立接手**的稿子，
新增 **§0「给接手的 AI：开工前必读」**：任务边界（**不引入新 bug 优先于多修一个 bug**）、
工作区状态快照、**绝对不要做的事**（按"违反了会出什么事"分四组）、备份纪律、
GitHub 令牌规矩、以及"怎么知道自己改坏了"的判据表；另加 §8「已经修过、别再修一遍」，专治好心重做。
**没有新建第二份交接文档** —— 这个仓库的规矩是同一件事只写一处。

### 同步文档里过期的测试项数

`AGENTS.md` / `README.md` / `tools/README.md` 里还写着集成测试 **41 项**、
e2e **135 项 / 14 个场景**，而实测已经是 **52 项** 与 **162 项 / 18 个场景**。
这几个数字正是接手的人用来判断"测试套件有没有被改动过"的，
写着旧值会让人误以为测试变过、甚至去查一个并不存在的回归。三处一并改成实测值并复跑 e2e 核对。

### 顺手补一条环境地雷：别用 shell 管道诊断换行符

在 `HANDOVER.md` §0.6 补了一条实测记录：这台机器上 shell 管道给出的换行符结论是**反的** ——
`grep -c $'\r' <文件>` 在**真正的 CRLF 文件**上返回 `0`，而放进 `$( … )` 里跑，
**纯 LF 的 3 行输入也会返回 3**。用错方法会把 LF 仓库"修"成 CRLF，
正是「用户入口守卫」要防的那类事故。判据只认 `git ls-files --eol` 或直接数字节。

### 发布时真踩到的坑：附件名被 GitHub 洗成 `default.pdf`

发布 1.1.2 时，附件 `使用说明.pdf` 上传**返回 201 成功**，但落在 Release 上的名字
变成了 **`default.pdf`**；而且从此**任何**非 ASCII 名字都返回 `422 already_exists`
（它们都被映射到同一个 `default.pdf`）。接口不报错、`state` 也是 `uploaded`，
**只有逐个核对附件名才会发现** —— 用户看到的就是 Release 页面上一个叫 default.pdf 的附件。

- 上传用的名字改成纯 ASCII（`Omitone-manual.pdf`）；**仓库里的文件仍叫 `使用说明.pdf`**
- `verify` 现在会逐个核对附件名是否与 `ASSETS` 声明一致（只看 `state=uploaded` 会放过它）
- `check.js` 的「用户入口守卫」加一条：`ASSETS` 里的附件名含非 ASCII 直接报错（**已反向验证**）

测试：自检 10 → **11 项**；集成 52 项、真实 Edge e2e 162 项不变，全绿。

---

## 1.1.1 — 修掉"提示词压太短反而更贵"，并把扩展瘦身 22%

> 1.1.0 发布后真机反馈：**DeepSeek 的输入几乎全是未命中缓存**，账单偏高。
> 查下来是我们自己前几轮"压提示词"压出的反效果 —— 详见下面第一节。
> 同一轮顺带做了体积体检（扩展解压后 963 KB → 751 KB）并补了图文说明书与接手索引。

### 前缀缓存：把提示词压得太短，会让每一分输入都按原价计费

DeepSeek 的上下文缓存**不是**按固定长度命中的，而是要求**请求前缀完整匹配**
某个已持久化的「缓存前缀单元」；单元来源之一是**跨请求被识别出的公共前缀**
（官方 Context Caching 的 Example 2：同一 system + 变动 user，要等到第二次请求之后
才把这个公共前缀持久化成单元，第三次才命中）。命中部分按约 1/10 价计费。

我们的请求当时长这样：

- 稳定内容只有 3 行 system（约 50 token）
- **user 消息的第一行就是随时在变的题目**
- `禁:`（历史错误答案）插在**每道题中间**，题目块里任意一处变动都会让前缀从这里分叉

→ 公共前缀既短又不干净，**无法被识别成缓存单元，命中率恒为 0**。
输入总量确实被我们压小了，但每一分都按原价计费 —— **省 token 省出了更高的账单**。

修法：

- **提示词重排为「稳定头 → 题目块 → 易变尾」**（`libs/prompt.js`）：
  输出格式说明提到最前且对所有请求逐字节相同；`禁:` 移到消息末尾，
  格式变成 `禁:题号=答案;题号=答案`，不再污染前缀
- **重试时整批重发**（`page.js`）：原本会把"已知正确"的题从中间删掉，
  前缀从删除处断掉、整段缓存全废。现在同一份卷子在 30 分钟内重试时保留完整题目列表，
  重试的输入是上一次的**超集且前缀一致**（官方 Example 1 的 `A+B` → `A+B+C`），整段命中。
  保险起见只在"刚发过请求"时才整批重发，避免第一次提问就多带题
- **把命中情况写进日志**（`content.js`）：`llm prefix cache` /
  `llm prefix cache all missed`，带上 `prompt_cache_hit_tokens` 与命中率。
  没有这个数字，"改动到底有没有让缓存失效"只能靠猜 —— 我们正是这样翻的车

实测（`npm run bench`，10 题一批）：

| | 数值 |
| --- | --- |
| 稳定前缀（system + 消息头） | **84 token**（此前约 50，低于可识别粒度） |
| 同批重试前缀重合 | **474 / 477 token（命中率 99%）** |
| 等价费用（命中按 1/10 价） | **降 89.4%** |

`tools/prompt-bench.js` 新增「前缀缓存可命中性」一节守住这两个数，
改提示词时输入变短不再是唯一指标。

### 体积：347KB 的明文映射表 → 122KB 紧凑二进制

扩展解压后从 **963 KB 降到 751 KB（−22%）**，zip 从 362 KB 降到 322 KB。
最大的一块是字形反爬映射表：`resources/table.json` 是 20902 条
「32 位哈希 → 码点」映射，347 KB 的文本全是十六进制与十进制数字，**占整个扩展的 36%**。

- 新增 `libs/font-table.js`：定义紧凑二进制格式（`OMT1` 头 + 排序哈希 uint32 数组 + 码点 uint16 数组，
  每条 6 字节），并提供编码、解码、"明文对象包成同一接口"三个入口
- 新增 `tools/table-pack.js`：`--pack` / `--verify` / `--unpack` / `--stats`
- **`table.json` 仍是唯一真源**（可读、可维护），`table.bin` 是生成物；
  `npm run check` 会**逐条比对**两者，不同步直接失败 —— 只比大小会把"内容错位"放过去
- 运行时优先读 `.bin`（122 KB，且不再造出两万多个字符串键的字典对象，
  省下约 1.5~2 MB 堆内存），源码目录没有 `.bin` 时**自动退回**读明文 json
- `tools/build.js` 打包前自动重新生成 `.bin`，并把 `table.json` 排除出扩展包
- 打包产物 750.9 KB / zip 321.8 KB

顺带清掉的重复与开销：

- 抽出 `_resumeVideoAfterOverlay()`：弹题与「继续学习」两条分支原本各写一份恢复播放逻辑，
  稍有出入就会变成"弹题这条能恢复、继续学习那条不能"这种只在真机才看得出的差别
- `_activePopupBlock()` 加 400ms 结果缓存（弹窗检测要遍历所有文档，主循环 250ms 一轮）
- `_findContinueStudyButton(video)` 接受传入的 video，省掉一次全文档查找；
  可见性判定挪到文案比对之后（它会触发强制重排）

### 文档

- 新增 **`docs/manual.html` + `docs/Omitone-manual.pdf`**：12 页图文使用说明书，
  含 4 张界面/流程示意图（全部矢量，缩放印刷都清晰），面向完全不懂技术的使用者
- 新增 **`HANDOVER.md`**：接手索引 —— 当前状态、按症状找文件的速查表、
  常见任务的固定动作、环境地雷、待办清单。**刻意不重复技术细节，只做导航**，避免文档互相分叉
- README 新增「体积账本」与「前缀缓存」两节，并挂上说明书与接手索引的入口
- **合并 README 里重复的「已知缺陷与重要限制」章节**：同一份清单曾出现两次
  （开头详版 8 条 + 中段简版 8 条），两版内容还有冲突（一处写明文表 347KB、
  一处写已换二进制），读者不知道信哪份。删掉中段简版、保留开头详版，
  并修正指向该章节的两处锚点与「第 N 条」引用（关思考=第 2 条、正确率=第 3 条，
  原来引的是「第 2 条」但按详版编号那其实在说正确率）

测试：自检 9 → 10 项（新增映射表同步校验）、集成 41 → **52 项**
（新增映射表二进制/明文逐条等价、坏数据必须返回 null 的反向验证）、
真实 Edge e2e 135 → **162 项**，全绿。

---

## 1.1.0 — 功能收官，转入长期维护

> **这一版是里程碑：该有的能力已经齐了，后续只修 bug 和跟随学习通改版做适配，不再堆新功能。**
>
> 内容上等于 1.0.12 的两项修复 + 一轮代码精简与服务商预设收敛，
> 所以从 1.0.11 及更早升上来的，相当于一次拿到全部修复。

### 只保留实测过的服务商预设（本轮最影响使用的一处）

早期内置了 9 家厂商预设：MiniMax / DeepSeek / Gemini / Claude / 通义 / 智谱 / Kimi /
OpenRouter / SiliconFlow。它们全是**照官方文档配好、但没在真实答题链路上验证过**的，
而且模型名是**钉死的快照** —— 厂商一发新版就失效。

结果很糟：用户照着下拉框一路填完，最后发现根本用不了。**这比留空白更坑人。**

现在只保留三种：

| 接入方式 | 状态 |
| --- | --- |
| **DeepSeek** | 唯一在真实答题链路上完整验证过（抠题 → 作答 → 回填 → 交卷 → 记分），**并作为默认值** |
| **Anthropic Claude** | 走独立协议（`/v1/messages`），协议适配有集成测试覆盖，未在真实题库上跑过 |
| **Google Gemini** | 走独立协议（`generateContent`），同上 |

配套改动：

- **默认服务商从 MiniMax 改成 DeepSeek** —— 默认指向一个没验证过的服务商本身就说不通
  （`content.js` / `page.js` / `popup/popup.js` 三处默认配置一起改）
- 用别家请选「自定义 OpenAI 兼容」自己填地址与模型名
- **老配置不会丢**：已删除的预置（如老版本存的 `minimax`）在载入时归一化 ——
  认识的预置照用，不认识但填过 API URL 的归到「自定义」并**保留原有地址与模型名**。
  这里踩过一个坑：不归一化时 `presetId !== "custom-openai"` 仍然成立，
  会拿空字符串覆盖用户已填好的 URL 与模型名，等于把人家的配置清空
- 弹窗新增一行说明，直接告诉用户"为什么只剩这几家、别家怎么填"

### 代码精简与提效

- 抽出 `_resumeVideoAfterOverlay()`：弹题与「继续学习」两条分支原本各写一份恢复播放的代码，
  逻辑稍有出入就会变成"弹题这条能恢复、继续学习那条不能"这种只在真机才看得出的差别
- `_activePopupBlock()` 增加 400ms 结果缓存：弹窗检测要遍历所有文档，而主循环每 250ms 一轮，
  这是持续开销。缓存的是**经过判断之后**的结果，所以"已答放行"不会被缓存绕过；
  进入放弃窗口时立刻作废缓存，否则"放弃"会被旧结果旁路掉
- `_describePopupQuiz()` 复用调用方已经抠到的选项，失败路径不再重复抠一遍
- 「继续学习」搜索把 `visible()` 挪到文案比对之后（它读 `offsetParent` / `getBoundingClientRect`，
  会触发强制重排），并把已取到的 video 传下去，省掉一次全文档查找

测试：e2e **162 项**全绿，其中新增"全新安装默认值"（清空 storage 后重载弹窗再验，
避免被前面场景写进 storage 的残留配置骗过）与"切到自定义不清空已填值"两条反向验证。

---

## 1.0.12 — 修复视频内弹题「AI 扫描但不填空 → 永久空转」

> 这是 1.0.11 的**缺陷修复版**。1.0.11 会保留为独立 Release，随时可回退。

真机反馈：Edge 后台刷课时，**部分课程在视频里弹出的题，AI 一直在扫描却从不填空，
课程永久空转**。查下来是四层缺陷叠加，任何单独一层都不足以致死：

真机反馈：Edge 后台刷课时，**部分课程在视频里弹出的题，AI 一直在扫描却从不填空，
课程永久空转**。查下来是四层缺陷叠加，任何单独一层都不足以致死：

1. **弹题请求没有任何去重**：tick 每 250ms 一轮，弹窗不关就每轮重新发一次 LLM 请求。
2. **认不出选项字母**：`_matchOptionItem` 只在选项带 `.num_option` 徽标或 `aria-label`
   时才知道字母；视频弹题是原生 `li + <input value="A">` 结构，字母恒为空串，
   模型回答裸字母 `"A"` 时**一个选项都匹配不上**。
3. **抠不出选项**：`_getOptionItems` 遇到没有 `Zy_/Cy_`、没有 `<label>` 的结构
   直接返回 `[]`，模型拿到的是一道"没有选项"的题。
4. **题型误判 + 流程死锁**：弹题题型靠**题干关键字**猜（含"正确/判断"就判成判断题），
   「下列说法正确的是？」这类单选题被误判；而弹窗分支在 `_runTick` 最前面 `return`、
   `_handleVideoPause` 又规定"有弹窗就不恢复播放" → 彻底转不动。

修复：

- 新增 `_inferOptionLetter(item, index)`：徽标 → 属性 → `input.value` →
  文本前缀 → **位置兜底**（仅当所有选项都认不出字母、且选项数在 2–6 之间时启用）
- `_getOptionItems` 增加"原生结构"兜底（`[class*="option"]` / `[class*="choice"]` / `li`），
  **只取最内层节点**，避免把整个选项容器当成一个选项
- 新增 `_detectPopupQuizType`：控件形态优先；判成"判断题"还需选项文本真的是
  「正确/错误」这类两两对立的表述
- 新增 `_activePopupBlock()`：统一"该不该拦住刷课"的判定，内含**放弃窗口**
  （`_popupQuizBlockedUntil`，60 秒）与**已答放行**（`_popupQuizSolvedKey`，30 秒）。
  `_runTick` 与 `_handleVideoPause` 全部改走它 —— 关键点：放弃后必须让播放和跳章
  继续跑，否则"放弃"没有任何意义
- `_handlePopupQuiz` 增加弹题指纹计数，同一道**最多问 3 次**模型，超限走
  `_giveUpPopupQuiz`：写 error 日志（**含弹窗 DOM 快照**，便于补模板）→ 点「跳过/关闭」→ 冷却
- `_fillPopupAnswer` 改为**返回是否真的选中了选项**；没选中就不点提交
  （空答点提交只会换来一句"请选择答案"，弹窗原地不动 —— 空转就是这么来的）；
  无选项但有输入框时走 `_fillText` / `_fillTextarea`（填空弹窗）
- `_checkPopupQuiz` 排除含 `<video>` 的节点（`.vjs-overlay` 这类播放器浮层极易误判），
  宽泛兜底分支必须**同时**看到"可点的选项"才认
- 新增诊断命令 `xxtAI.diagnosePopup()`

**行为上的取舍**：它现在**不会**为了答对一道不认识的弹题无限重试 ——
最多问 3 次就放手，保证课程继续。这是刻意的：卡死一节课的代价远大于漏答一道弹题。

**续：弹题答对了也照样卡 —— 漏点了「继续学习」**

上一轮把弹题答对之后，真机上又暴露出**第二个卡点**：弹题答完（或视频被判定为挂机）后，
**播放器右下角会冒出一个「继续学习」按钮，点它才回到正常播放页**。
代码里完全没有这个按钮的处理：它不属于弹窗题（没有选项），也不属于任何已有分支，
于是主循环每轮都跳过它，视频一直停在那儿 —— 表现和上一轮一模一样：AI 在跑，课程不动。

- 新增 `_findContinueStudyButton`：按文案（`继续学习` / `继续观看` / `继续播放`）
  加位置（与视频同文档）加形态打分定位，**叶子节点优先** ——
  站点把 `onclick` 挂在按钮本身，点到外层浮层是没反应的
- 新增 `_tryContinueStudyPrompt`，挂在 `_runTick` 中**弹题之后、播放之前**
  （顺序反了就永远轮不到它）
- 两道保险：同一个按钮 3 秒内只点一次；连点 5 次还在说明点了没反应，
  停 60 秒并写日志 —— 免得把"点不动的按钮"变成新的空转源

测试：新增 5 个真实浏览器场景（原生弹题选项识别 / 弹题答题往返且已答不重复问 /
填空弹窗 / 失败放弃不死循环 / 继续学习提示及其反向验证），e2e 由 135 → **157 项**全绿。

文档：README 新增「已知缺陷」第 8 条、易错点清单 #31、排查表新行，
以及调试命令 `xxtAI.diagnosePopup()`。

---

## 1.0.11 — 提示词收敛与工程化

### 1.0.11 补丁：效率与 token 优化 + 测试脚手架修复

**空答案补问（本轮最主要的 token 优化）**

原来的行为是：模型偶尔对某道题给出空答案（空串 / `null` / 位置错位被丢弃），
`page.js` 的 `_avoidKnownWrongAnswer` 会走到**空答案兜底 —— 直接猜第一个选项**。
判断题猜错的概率是 50%，一旦猜错就触发"整卷带 `禁:` 前缀重答一遍"：
几百 token + 一整轮"填→提交→判错"的页面往返，还会拖慢整章进度。

现在 `handleLLMRequestDirect` 在一轮跑完之后再检查一次：**只对没拿到答案的那些题**
打包成一次小请求重问（日志 `llm refill unanswered`，失败记 `llm refill failed`）。
把"猜"换成"问"，成本通常只有一两百 token，正确率同步改善。

要点：

- 只在**部分成功**时补问 —— 全部失败说明 API/解析本身有问题，
  `requestAnswersWithRetry` 已经重试过两次，再问是白烧
- 补问与主批共用同一个 `mergeAnswers`（抽出局部函数），对齐规则不会漂移
- 补问失败不会污染 `apiConnectionFailed`：后面的成功分支会把它重置回来
- 集成测试新增用例覆盖这条路径（41 项）

**DeepSeek 关掉思考模式**

V4 系列默认开启思考，答一道选择题要先烧约 200 个推理 token。
请求体加 `thinking: { type: 'disabled' }`（实测 2 题 361 → 133 token）。
**只对 DeepSeek 加** —— 其他 OpenAI 兼容服务遇到未知参数会直接 400。

**良性告警降级**：`submit confirm key mismatch` 从 `warn` 降为 `info`。
提交确认弹窗出现时 URL 可能已经变了，而测验本就就绪、按设计继续提交，
它不是故障线索，却很容易被当成故障排查。

**测试脚手架：扩展 ID 不再靠"算"**

`npm run e2e` 全线失败，14 个场景都报「page.js 在真实 Edge 中加载成功：失败」，
页面里却一条异常都没有。查下来**错不在扩展**：

- 扩展 ID 的算法是 `SHA256(目录绝对路径, UTF-16LE)`，**对路径大小写敏感**：
  `D:\Omite` → `hdlemlcmf…`（真实），`d:\Omite` → `locncobd…`（错）
- 从 Git Bash 风格的 cwd（`/d/Omite`）启动 node，`__dirname` 的盘符变成小写，
  哈希整个错开 → 测试拿着错误的 ID 去注入 `page.js`（404）和打开 popup（错误页）
- 扩展其实加载得好好的，页面里 `[Omitone] content bridge ready` 照常出现

现在改为**运行时发现**真实 ID：content script 一跑就会有
`Runtime.executionContextCreated`，事件里的 `origin` 就是 `chrome-extension://<真实ID>`；
其次是 `/json/list` 里 title 含 Omitone 的 target；路径哈希降级为兜底，
两者不一致时会打印一行警告。

**工程杂项**：`npm run check` 排除 `.debug-profile` / `.workbuddy`（CDP 调试用的
Edge 独立 profile 与本地记忆目录），消除长期存在的"游离 JS 文件"误报。

### 1.0.11 补丁：真实页面联调（CDP 直连用户的浏览器）

用 `--remote-debugging-port` + 独立 profile 只读观察真实学习通页面后定位到两个 bug：

**`_detectQuiz` 的标题匹配导致章节永久卡住**

`if (title.indexOf('考试') !== -1) return true` —— 只要章节标题里有"考试"二字就判定
"这里有测验"。名为「10.1 课程考试」的章节（0 个任务点、早已完成）因此被判成有待答的
测验，`_isCurrentCompleted` 拒绝跳过，插件陷入 5 秒一轮的
`study begin → quiz handler → hold` 死循环，不断刷 `quiz scan found 0 questions`
（正是用户描述的"卡住"）。

现在判定**必须有证据**：标题命中之外，还要在 URL 链（`location.href`、主 iframe 的
`src`、文档中所有 iframe 的 `src`）里真的看到 `ananas/modules/work`、`api/work`、
`exam/test`、`testpaper`、`selectWorkQuestion` 等作业/考试页特征。

**API 表单只有点「保存 API」按钮才落地**

`apiUrl` / `apiKey` / `model` / `captchaModel` 只在点保存时才写入，
而 **`apiType` 下拉根本没有 change 监听** —— 改了接入方式若不重填一次
就不会保存。结果是 `apiKey` 为空 → `enableQuiz` 的硬守卫跳过全部答题，
症状是"AI 完全不听题"，很容易被误判成扫描失败。

现在输入类字段 800ms 防抖自动保存，`apiType` 也挂了监听，并给出 toast 提示。

**模型预设更新至 2026-09 现行版本**

 moonshot-v1-8k 已于 8 月 31 日下线（它的"失效"表现就是请求 400）。
全部预设重新核对官方文档：MiniMax-M3 / gemini-flash-latest（唯一真正的常青别名）/
claude-sonnet-5 / glm-4.7-flash / kimi-k2.6 / deepseek-v4-flash / qwen-plus。

**逻辑审计：修掉三处判断错误（其中两处是本次新引入的）**

对"任务点该不该做 / 做不完要不要放弃"这条链路做了完整复查：

- **`isPassed` 排在 `job` 之后 → 已通过的任务点被重做一遍**。
  两者同时为真时先判 `job` 就返回 `'job'`，调用点看到 `'job'` 直接开跑。
  长视频尤其致命（等于白播一整遍）。现在 `isPassed` 优先返回 `'finished'`，
  而"重学模式"本来就会把 `'finished'` 也当作要重做。
- **"没完成"就计数 → 必做任务点会被误跳过**。这是上一轮引入的判断错误：
  长视频一次本来就跑不完，`_isJobCompleted` 会一直返回 false，
  连续几次就会被记入放弃名单。现在改为**只有"两次进度快照完全一致"才认定卡住**，
  并新增 `_taskProgressSnapshot`。
- **不可滚动的页面快照恒为 `scroll:0`** —— 把"测不到"误当成"没进展"。
  现在只有**确实可滚动**（`scrollHeight - clientHeight > 20`）的文档才用 `scrollTop` 当进度，
  否则返回空串，调用方一律不计。
- **讨论任务打不开也先把"已完成"标记写死了**。`_markDiscussionDone` 先于打开动作写入
  （为防止重复打开），但若一个入口都没打开成功，该任务点会被静默跳过 24 小时、
  日志里毫无线索。现在新增 `_unmarkDiscussionDone` 撤回，并留一条 warn 日志。

**测试脚手架：连跑会假失败**

连续跑两次 `npm run e2e` 时，上一次的 Edge 被杀掉后调试端口未必立刻回收，
下一次会连到**正在退出的旧实例**上，症状是"所有注入都失败"或"端口仍被占用" ——
看起来像随机偶发失败。现在收尾与启动前都会等端口真正释放（12 秒超时），
连跑三次稳定通过。

**文档：README 重写为完整交接文档**

用户明确要求"便于下一个 AI 修复和 debug"，因此把 README 从「功能清单」扩成**交接文档**：

- 第一屏给出「按任务跳到哪一节」的索引与三条硬规则
- **§5 代码易纠缠点**：`_runTick` 仲裁顺序为什么不能动、互斥开关全表、
  两个重载源、配置三份默认值、位置式输出协议、答案形态规整、
  任务点判据顺序、放弃机制三条铁律、讨论任务四个易踩点
- **§6 易错点清单（25 条）**：每条都是"症状 → 根因 → 现在怎么防"，
  全部来自真实踩过的坑，越靠前越贵
- §7 症状→病因对照表；§8 改哪里定位表；§9 新增配置项完整清单
- `AGENTS.md` 收缩为「守则 + 索引」，技术细节全部交给 README，避免两份文档分叉

**修复：多个讨论任务点会互相顶掉 / 整批漏做**

用户反馈"遇到多个讨论任务可能会出现 bug"，定位到三处：

- **`#isFinished` 用了文档级查找**。`_collectDiscussionTargets` 在卡片循环里调
  `doc.getElementById('isFinished')` —— 一个章节里多张讨论卡片若落在同一个文档中，
  它们会**共用同一个"已完成"标志**：第一张已完成，其余全被当成"已回复"而**永久跳过**
  （反之则是反复重开同一个）。现在按卡片范围查找：先看卡片自身，
  再逐层向上，且**只在"只包含本卡片"的祖先上读标志** ——
  共用容器必须先判断卡片数量再读，顺序反了就等于原 bug 复发。
- **去重键用 `url.slice(-70)`**。两个任务的地址若只有前段不同、后 70 字符相同，
  会算出同一个键 → 第二个被 `_isDiscussionDone` 判成"24 小时内已处理"而**永久漏做**。
  改为优先取真正唯一的 `mtopicid`，取不到再退回整条 URL 的哈希（djb2）。
- **兜底路径的键是 `名称 + href.slice(-40)`**，同样会碰撞，一并改为哈希。

安全性权衡：卡片范围找不到标志、且同文档有多张卡片时，按**未完成**处理
（宁可按未完成也不漏做）；文档里只有一张卡片时保留旧的文档级查找，行为不变。

**修复 + 新增：跳过不必做 / 做不完的任务点**

用户反馈"有些任务不需要完成，老师会安排不可滑动来挡住插件，浪费时间"。对照开源实现
`cxmooc-tools` 的 `CxTask` 找到了两条判据：

```js
if (this.taskinfo.job) { this.done = false }   // 是任务点 → 要做
else { this.done = true }                       // 不是任务点 → 直接跳过
```

- **`job: false` 被误当成任务点**（真 bug）。`_getAttachmentWorkType` 里
  `job === true` 有专门分支，但 `job === false` 会**直接落到"按模块名推断"**那段 ——
  于是"老师没设为任务点的视频/文档"又被推断成 `job`，插件去白做一遍。
  现在显式 `job: false`（含字符串 `"false"`）在此处直接返回 `not-job`，
  `_buildAttachmentOnlyJob` 也会返回 null；`_classifyTaskFrame` 同样识别
  帧数据里的 `"job":false`。判据与参考实现对齐。
- **新增"做不完就放弃"机制**。防拖拽视频、不可翻页的文档、不计分的摆设，
  插件对这些只会一次次重试。现在每派发一次任务点就检查是否真的完成，
  没完成才计数；连续 `taskGiveUpAttempts` 次（默认 4）未完成则记入 localStorage
  （24 小时）并跳过，同时留一条 warn 日志说明原因与如何清除。
  安全点在于「拿不准就当完成」：`_isJobCompleted` 判断不了时一律返回 `true`，
  **正在推进的长视频不会被误放弃**。
- 新增 `xxtAI.taskGiveUpList()` / `xxtAI.clearTaskGiveUp()`，
  用户可查看与清除这份名单。

**提示词 v2：位置式输出 + 加大分批量（token 再降 27%）**

在 v1 基础上继续压缩，**40 题整卷 token 合计 2856 → 2076**（相对 1.0.10 累计省 50.5%）：

- **输出改成纯位置式数组** `["A",["A","C"],true,"x|||y"]`，不再回传 `i`/`a` 键名。
  答案与题目按位置对应 —— 这本来就是 content.js 在 index 缺失时的兜底行为，
  现在把它提升为唯一协议。每题输出约 11 token → 约 5 token，
  格式示例本身也短了一半。
- **分批从 5 题提到 10 题**。system 提示词与格式示例是每批都要重发的固定开销
  （约 88 token/批），题目正文无论分几批都只发一次。40 题从 8 批降到 4 批。
  开源实现 `cxmooc-tools` 的题库接口一批是 20 题，10 属于保守取法。
  `tools/prompt-bench.js` 现在会打印 5/10/20 三档的固定开销对比，便于将来调整。
- 验证码提示词去掉"这是网页校验用的图片""仔细识别"等不改变行为的铺垫。
- 三次完整复查（自检 / 基准 / 集成 / 真实浏览器）全部通过且稳定。

**为保证"压缩不压掉正确性"新增的防线**

- `normalizeItem` 的三种输入形态各有一条断言（位置式 / `{i,a}` / 旧键名），
  其中**位置式数组** `["A","C"]` 是最容易写错的一例 ——
  数组的 `typeof` 是 object，若被当成 `{a:…}` 包装对象解析，多选答案会被整条丢掉。
- `content.js` 新增 `coerceAnswerForType`：单选收到 `["A"]` 解包成 `"A"`，
  判断题收到 `"正确"`/`"T"` 归一成 `true`；多选数组**必须原样保留**。
  这类偏差若原样交给 page.js，匹配不上 → 静默不填 → 永不提交。
- 位置式输出长度与题目数不符时记 `llm answer count mismatch` 日志，
  按位置尽力对齐；没对上的题保持未填，因此**不会误提交**，下一轮 tick 重试。
- `tools/integration-test.js` 的假模型默认改为返回位置式数组（真实协议），
  同时保留 v1 对象协议与旧键名的兼容性用例；`tools/browser-e2e.js` 的假模型
  也改为位置式，并用真实 Edge 跑通 5 种题型的完整回填。

**提示词 / token（v1）**
- 新增 `libs/prompt.js` 作为提示词唯一真源（同时可在 Node 中被 `require`，便于测试）。
  `content.js` 里原有的 `buildSystemPrompt` / `buildQuestionsText` / `buildOutputFormat` 全部移出。
- 用「题型代号 + 一行图例」取代逐题 `Question N [type]:` + `Options:` 前缀，
  用单行格式示例取代按题生成的 JSON 样板，答案对象改用短键 `{"i":0,"a":"A"}` 且不再回传 `type`。
- 实测（`npm run bench`，o200k 分词器）：整卷输入 **862 → 620 tokens（-28%）**，
  输出 **186 → 94 tokens（-49%）**。
- `page.js` 只把「确实还需要模型作答」的题发出去：已确认正确、本轮已填且 DOM 有值的题直接跳过。

**修复：题目扫描（"AI 扫描不到题目"的真正成因）**

在真实 Edge 的端到端测试里暴露出来的三处抠题缺陷：

- **题干被清成空字符串**。学习通把题号单独放在 `.fontLabel`（内容就是 `"1."`），
  旧实现拿到第一个命中的标题选择器就 `break`，剥掉序号后题干变成空串 ——
  模型只看到选项、看不到问题，只能瞎猜。现在要求候选清洗后长度 ≥ 4，
  不合格继续试下一个选择器，最后退回容器文本并在第一个选项标记处截断。
- **容器的选择器循环无条件 break**。命中一批节点后还要过可见性/文本过滤，
  过滤后可能一个不剩（占位容器、隐藏模板节点），旧实现此时直接返回 0 题。
  现在改成挨个选择器试，谁第一个给出**过滤后非空**的结果就用谁。
- **选项切分的正则写错**。`textOf()` 已把换行压成空格，`[A-F][.、．\s]+[^\n]+`
  会从第一个选项标记一路吞到字符串末尾，把 A~D 全塞进同一个"选项"里。
  改为先定位所有选项标记再按区间切分。

新增题目扫描诊断：扫到 0 题时自动写进运行日志，也可在页面控制台手动执行
`xxtAI.diagnose()`（完整报告）或 `xxtAI.scanQuiz()`（只抠题）。
报告会指出断在哪一环：没命中选择器 / 容器被过滤 / 容器找到了但解析全失败。

**修复（其他）**

- `page.js._extractOptionText` 的正则里 `閫夐」` 是 `选项` 的 GBK 乱码残留
  （UTF-8 字节被按 GBK 解码），导致以「选项」开头的选项文本永远剥不掉前缀。
- `_getQuizTitleKeyFromElement` 同样会因为只拿到题号而产生退化缓存键。
- `popup.js` 切换「接入方式」时只改表单不回写 storage，重新打开弹窗会跳回旧值 —— 改为选中即保存。
- `content.js.apiFetch` 直连兜底分支误用 `timeoutMs` 而非归一化后的 `timeout`。
- `content.js` 的分批映射不再用 `批号 × 5` 反推全局题号（该算法隐含"发来的题就是全部题"的假设），
  改为原样透传调用方给的 `index`；模型回传序号越界或缺失时按数组位置兜底，并按 index 去重。
- 解析容错同时接受新旧两套键名（`i`/`a` 与 `index`/`answer`）。
- `manifest.json` 与 `page.js` 中遗留的 UTF-8 BOM 已清除。

**工程化**

- 建立 git 仓库（此前无版本控制），并保留 1.0.10 基线提交。
- 新增 `npm run check`：JS 语法、manifest 解析与 BOM、引用文件存在性、
  **版本号三处一致性**、提示词单一真源、**编码损坏探测**、死代码探测。
- 新增 `npm run itest`：把真实 `content.js` 加载进 vm 隔离环境（打桩 chrome.*），
  覆盖稀疏 index 透传、旧 schema 兼容、解析失败分类、三种模型协议（27 项）。
- 新增 `npm run e2e`：独立临时 profile 启动真实 Edge 加载扩展，配合本地 mock 测验页
  与 mock 模型接口，跑通「注入 → 抠题 → 提示词 → 模型 → 回填」（15 项）。
- 新增 `npm run bench`（提示词 token 基准，含兼容性与信息完整性自检）、
  `npm run build`（零依赖打包，产出可加载目录与 zip）、`tools/fix-bom.js`、`tools/ext-id.js`。
- 新增 `.gitignore` / `.editorconfig` / `package.json`。
- 文档：`README.md`、`ARCHITECTURE.md`、`AGENTS.md`（含"哪里会代码打架"仲裁说明）、
  `CHANGELOG.md`、`tools/README.md`。
- 死代码处置：19KB 的 `background-core.js` 移入 `legacy/`（从未被加载，且含第三份提示词实现）；
  删除 `popup.js` 中已失效的 `testApiConnection()`（它依赖的 handler 只存在于 legacy 文件里）。

---

**修复：真实浏览器交叉检验挖出的产品缺陷**

新增 `npm run e2e`（真实 Edge + 9 个 mock 页面 + mock 模型，65 项断言）后，
又暴露出四个用打桩测试根本测不到的缺陷：

- **幽灵调用导致验证码链路整体失灵（最严重）**。`_checkCaptchaDialog` 末尾调用了
  `this._diagnoseBlockedPage()`，而那个函数在早前一轮"清理调试代码"时就被删掉了 ——
  于是「没有验证码」这个最常见的情况下每次都会抛 `TypeError`。
  4 处调用点里只有 `_runTick` 那处包了 `try/catch`，另外 3 处
  （`_handleCaptchaDialog` 复检、`_backgroundCaptchaTick`、`_handleVideoPlay` 守卫）
  直接把链路打断。`tools/check.js` 新增**幽灵调用检查**，这类问题以后会被自检拦住。
- **文档解析缺兜底，导致永不交卷**。`_resolveQuizAnswerDocument` 与
  `_resolveQuizSubmitDocument` 在页面没有 `#iframe` 时直接返回 `null`，
  连锁后果是 `_getQuizQuestionFilledValue` 返回空串 →
  `_areQuizAnswersFilled` 永远 false → 答案填好了也**永远不提交**，
  同时 `_shouldAutoSubmitQuiz` 被永久禁用。两者都补上 `|| document` 兜底。
- **简答题判定"未填写"**。隐藏答案域存在但为空时不再直接返回空串，改为回退到可见控件
  （简答走富文本编辑器，页面自身 JS 未必把内容同步进隐藏域）；
  `_fillTextarea` 也补上写隐藏域，与 `_fillText` 行为对齐。
- **`textOf()` 压平换行导致选项切分错误**（见上）。

**对照开源实现校正 DOM 选择器**

以 `cxmooc-tools`（`src/mooc/chaoxing/question.ts` / `vcode.ts`）为参照校正，
补上了之前只覆盖课程页、漏掉作业/考试页的整套标记：

- 新增作业/考试页的题目容器 `.Cy_TItle` 及其变体选择器
- `_getOptionItems` 补上 `.Zy_ulTop/.Zy_ulBottom/.Zy_ulTk`、`.Cy_ulTop/.Cy_ulBottom/.Cy_ulTk`
  —— 并且**让 `<li>` 排在 `<label>` 之前**：`qid` 挂在 `<li>` 上，
  返回 label 会导致隐藏答案域永远为空、判定未填写、永不提交
- `_extractOptionText` 补上 `<a>` 分支（作业页选项文本包在 `<a>` 里）
- **「记住正确答案」链路修正**：批改结果在 `.Py_answer` / `.Py_tk`，
  对错图标是 `.fr.dui` / `.fr.bandui` / `.fr.cuo`。旧实现只认
  `marking_dui` / `correct-icon` / `right-icon`，在真实页面上一个都命中不了，
  于是答案缓存从未生效、每次答题都要重新问模型（白花 token）。
  新增 `_isQuizQuestionMarkedCorrect`（用 `classList` 精确比对 token，
  保守判定：宁可漏记也不把错题写进缓存）与 `_extractDisplayedCorrectAnswer`
  （交卷后输入域被清空，从展示文本里解析正确答案）。

**任务类型覆盖补全（三轮复查产出）**

按学习通真实的任务点 `module` 逐一核对后，补齐了此前会被**静默丢弃**的类型：

- **音频任务点 `insertaudio`**：`_classifyTaskFrame` 与 `_getAttachmentWorkType` 此前只认
  `insertvideo`，音频帧与音频附件会掉进 `other` 然后被整条丢弃。
  现在音频复用同一套媒体流程（`_playChaoxingMediaJob` 本就支持 `audio` 元素）。
- **图片任务点 `insertimage`**：此前同样被丢弃，现在按"阅读"处理（打开即算）。
  注意只在附件带 `job: true` 时生效 —— 推断路径**不**把图片当任务点，
  否则会去处理一堆纯展示图。
- **不支持的类型不再静默丢弃**：投票 `insertvote`、直播 `insertlive` 等
  会留一条 `unsupported task point type` 运行日志。此前用户只能看到"这个任务点没做"，
  完全无从判断是识别失败还是压根不支持。
- **扩展名匹配允许不带前导点**：学习通的 `property.type` 常见写法是 `"ppt"` / `"mp4"`，
  而旧正则 `\.(ppt|…)$` 只认 `".ppt"`，这类任务点会被判成 `not-job` 直接漏掉。

**代码精简**

- 抽出 `libs/api-url.js`，消掉 `content.js` 与 `popup/popup.js` 里各一份的
  API 地址构造与密钥清洗（约 40 行重复）。这两份**已经分叉**：只有 popup 那份会剥掉
  用户粘贴时带的引号，正是"弹窗测试连接通过、页面上答题却失败"这类现象的土壤。
- `_resolveQuizSubmitDocument` 与 `_resolveQuizAnswerDocument` 实现完全相同
  （解题域本来就是同一个文档），改为前者转发后者，消除两处各自演化的风险。
- `content.js` 新增 `missingModuleError()`：依赖模块缺失时返回
  "请在 edge://extensions 重新加载扩展"，而不是抛 `Cannot read properties of null`。

**测试与自检加固**

- `tools/check.js` 新增两项：**幽灵调用检查**（调用了但没定义）与
  **死方法检查**（定义了但无人调用，告警级）；唯一真源检查扩展到 API 地址构造，
  并校验 `manifest.content_scripts` 真的注入了 `content.js` 依赖的 lib。
- `tools/integration-test.js` 改为按 manifest 顺序加载全部隔离世界脚本
  （此前只加载 `prompt.js` + `content.js`，抽走 API 构造函数后立刻暴露）。
- `tools/browser-e2e.js` 扩到 12 个场景 / 100 项断言：新增**任务类型识别**
  （视频/音频/文档/图片/测验/投票的映射表 + 推断策略 + 不支持类型留日志）、
  **隐藏 audio 兜底**、**设置弹窗冒烟测试**（依赖加载顺序、地址构造、密钥清洗、
  初始化、无未捕获异常）—— popup 此前零测试覆盖。
- Chromium 的 `--disable-extensions-except` 与 `--load-extension` 必须成对使用，
  且扩展 ID 需按 `SHA256(路径 UTF-16LE) → a-p` 直接算出，不能等 CDP 报出来。

**文档**

- README 增加「功能与验证状态」表，逐项标注自动化验证覆盖到哪一步、
  哪些必须到真实课程页人工确认（真实编解码、加密字体、任务点结构）。
- `AGENTS.md` 新增：幽灵调用教训、学习通两套题目标记对照表、
  `_getOptionItems` 顺序约束、文档解析兜底、真实验证码选择器。
- `tools/README.md` 记录 browser-e2e 的 9 个场景与全部踩坑。

## 1.0.10 — 讨论任务点按真实浏览器观察重写

- 用独立临时 profile + CDP 只读观察真实讨论任务点，推翻此前所有猜测：
  讨论点在课程页里是三层嵌套 iframe，最内层卡片 `#topicMainDiv` 的 `data` 属性才藏着真实讨论地址
  （`groupweb.chaoxing.com`，独立跨域网址）；课程里共 18 处，模块名是 `insertbbs`。
- 之前靠"页面上有讨论字样的可点击元素"匹配 —— 而卡片文本是**题目内容**，
  既不含"讨论"也不在 attachments 名称里，所以永远找不到、被判为 other 跳过。
- `_findDiscussionTask` 改为三级策略（卡片直取 → 模块解析 → 文字匹配），并过滤 `#isFinished`，已完成绝不重发。
- 精确定位控件：编辑区 `.replyEdit textarea[placeholder="回复话题"]`，
  提交按钮 **`.addReply`**；页面上另有 `.replyBtn` / `.topicDetail_title_right`
  两个同名"回复"按钮其实是展开按钮，点错就永远发不出去。
- 成功判据改为「回复列表新增条目」为主，兼容成功文案与编辑区清空。
- 安全修正：讨论页/验证码页改为**只有 `shouldAutoStart()` 为真才自动运行** ——
  没在刷课时进入讨论区一律静默，绝不自动发评论。

## 1.0.9 — 讨论任务点自动完成

- 新增讨论任务：检测到后自动跳转、发布评论（内容可配）、完成后返回刷课页继续，带 90 秒超时防卡。
- 防重复三处：打开前即写 `omitone_discussion_done` 标记、`_discussionPosted` 保证每页只发一次、扫描/打开各自节流。
- popup 新增开关与「讨论回复内容」输入框。

## 1.0.8 — 音频自动最大倍速 + 静音播放 + 清理

- `_readRateMenuMax` 改为优先查播放器容器，音频不做整文档扫描（避免读到别的播放器的档位而误判）。
- 新增倍速探测任务纪元 `_rateEpoch`，修复"切换任务时旧视频探测结果串到音频上"与"探测中切任务后新任务永不探测"。
- 新增 `audioMuted` 配置（音频默认静音播放）。
- 删除调试期诊断转储函数与 2 处调用。

## 1.0.7 — 音频无法播放

- 根因不是 m4a 格式，而是**可见性过滤把隐藏 `<audio>` 全滤掉了**（自定义播放器常把 audio 隐藏，零尺寸）。
- 新增 `_pickMedia`：优先可见媒体，无可见媒体时回退隐藏的 audio。
- `_findMediaInDocument` 增加子 iframe 递归（深度 ≤3，走 `_safeDocOf` 防跨域抛错）。
- `_videoEventHandle` 补绑 `error` 事件 —— 此前格式不支持（code 4）与解码失败（code 3）永远静默。
- 新增 `_maybeRepairMediaSource`：m4a/MIME 兜底，页面内 fetch → Blob → objectURL 重设 src → 恢复进度。

## 1.0.6 — 支持独立验证码网址

- 验证码可能不在学习通界面里，而是独立网址。此前 manifest 注入范围只匹配学习通路径，那些页面**根本不会被注入**。
- `content_scripts.matches` 扩为 `*://*.chaoxing.com/*` + `*://*/*`，
  `web_accessible_resources.matches` 同步放宽。
- `content.js` 增加静默守卫：非学习通页面只有「正在刷课 + 疑似验证码页」才注入，其余立即退出、零行为。
- 新增独立验证码页模式（5 个方法，最多 4 轮，处理完 `window.close()` / `history.back()`）。

## 1.0.5 — 跨域帧异常打挂主循环

- 根因：`_searchChaoxingJobOcs` 与 `_ensureOcsStudyRunner` 裸读跨域窗口的 `document`。
  验证码/反作弊页常把主 `#iframe` 指向跨域地址，`SecurityError` 冒泡进 tick 的 catch，
  每 250ms 一次 → 日志被同一条错误刷满、后续所有逻辑不执行。
- 新增 `_safeDocOf` / `_safeWinDoc` / `_isFrameSameOrigin`，跨域一律返回 null 不抛异常。
- `_runTick` 重排：验证码检测提到最前并各自包 try/catch，任何单点异常不再打断整轮。
- 新增 `_checkBlockedByCrossOrigin()`：主 iframe 跨域持续 >20 秒则刷新页面恢复（180 秒冷却）。
- tick 错误日志 30 秒去重并附 `err.stack` 前 3 行。

## 1.0.4 — 验证码与 AI 答题彻底分离

- 拆出独立开关 `enableCaptcha`（验证码链路此前从不检查 `enableQuiz`，但用户以为被联动挡住）。
- `_checkCaptchaDialog` 重写为三层识别，并挪到 `_runTick` 最前。
- 新增 `captchaModel` 配置（验证码走独立的视觉模型）。

## 1.0.3 — 拖尾 / 填空 / 音频 / 配置同步

- 新增"可拖动视频直接拖到结尾"（`_trySeekToEnd`，每个视频只尝试一次）。
- **修复 AI 答题开关关不掉**：`content.js` 转发配置更新时漏了 `source: 'xxt_bridge'`，
  被 `page.js` 的过滤器丢弃 —— 症状是"改了开关必须刷新才生效"。
- **修复 manifest 缺 `background` 段**：service worker 从未注册，`api_fetch` 代理一直不可用，
  content script 的跨域 fetch 受 CORS 约束（历史上"API 连接错误"的隐患之一）。
- 修复填空题不提交（`_fillText` 只写可见 input，而读取走隐藏域 `#answer{qid}`）。
- 修复音频不自动播放（`_getVideoEl` 由 `video` 改为 `video, audio`）。

## 1.0.2 — 基线（1.0.2 之前的记录未留存）

已具备：视频自动播放/倍速/防暂停、单选/多选/判断/填空/简答/弹窗题、
文档与 PPT 翻页、字体反爬解密。
