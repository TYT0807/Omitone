# Omitone 1.2.9

## 改进了什么

**任务点找不到时，日志会说明卡在哪一步。**

以前遇到「这一页的任务点没被识别出来」，日志里只有一句"没匹配到" —— 想查原因只能靠猜。
现在它会**逐个页面框架**列出**被哪一条挡下来的**，例如：

- 框架里认不出任务类型
- 取不到任务点身份号
- 身份号在任务清单里找不到
- 是视频任务，但「媒体」开关是关的
- 这个任务点已经做过、或在"放弃名单"里

每条还会带上**是哪个框架**，方便对着页面定位。

## 对你有什么影响

- **插件的行为一个字没改** —— 这一版只改日志，日常使用完全感觉不到差别
- 只有"任务点没被识别"这种情况下的日志变详细了
- **如果你之前遇到过「某个任务点怎么都不做」**，这一版能让问题**一次就看出卡在哪**

## 要不要升

- **用得挺好、也没遇到任务点不被识别** —— 可以先不升，这一版不改行为
- **手头正好有"某个任务点不被识别"的情况** —— 建议升，然后把日志发出来，就能直接定位

## 升级方式

下载 `omitone.zip` → 解压覆盖旧目录 → 在 `edge://extensions` 点一下重新加载，
然后**刷新学习通页面（F5）**。
