# Omitone 1.2.7

修两处 **静默的错** —— 都不报错、不留日志，只是"行为悄悄不对"。位置都在 `libs/`。

## 一、API Key 里如果带引号，会被当成错的 Key

**症状**：Key 明明是对的，插件却一直报 **401**（连"测试连接"也失败）。

**根因**：清理 Key 的两步**顺序反了** —— 先剥 `Bearer` 前缀、后剥引号。
于是从配置文件里整段复制出来的 `"Bearer sk-xxx"`（**引号把 Bearer 一起包住**）：

- 先剥 `Bearer`：它以引号开头，匹配不上 → 原样留着
- 再剥引号：变成 `Bearer sk-xxx` —— **前缀还在**

真正发出去的就成了 **`Bearer Bearer sk-xxx`** → 服务端一律 401。

> 为什么这条值得单独修：症状是"Key 是对的却报 401"，而插件现成的排查提示会把你引向
> **"Key 填错了 / 额度没了"** —— 方向完全错。而 `"Bearer xxx"` 恰恰是从配置文件
> 复制粘贴最常见的样子。

**已修**：先剥引号、再剥 `Bearer`，并顺带覆盖了双层包裹（`'Bearer "sk-xxx"'`、
`"  Bearer sk-xxx  "`）和"只粘了 `Bearer` 没粘 Key"这类手滑。
**键名里本来就带 `Bearer` 字样的不会被动**（有反向对照测试守着）。

## 二、模型回的答案可能被错配到第一题

**症状**：偶尔有一题拿不到答案 → 插件会多跑一轮"补问"，**多花一次请求的钱**。

**根因**：解析模型返回的序号时直接用了 `Number(...)`，而 **`Number(null)` 和
`Number('')` 都是 0**。于是模型显式回了 `"i": null`（意思是"我没给序号，按顺序对"）时，
被当成了 **第 0 题** —— 那条答案被硬塞给第一题，真正该拿它的那一题落空。

**已修**：`null` / `空串` 现在会正确地按"顺序对齐"处理。
注意 **`i: 0` 是真的第一题**，不会被误当成"没给序号"（有反向对照测试守着）。

## 对你有什么影响

- **装法、用法、界面、下载方式全都没变**；密钥、设置、已记住的答案都还在
- **如果你的 Key 一直能用，那这两条你都碰不到** —— 它们是特定输入下才触发的
- **如果你的 Key 填了却总是 401，这一版很可能就是你要的**：把 Key 重新贴一次
  （不用改任何东西），或者直接升级后重试
- 升级方式：下载 `omitone.zip`，解压覆盖旧目录，然后在 `edge://extensions` 点一下重新加载

## 怎么确认没改坏

- `npm test` 全绿：自检 **17** 项 · 提示词基准 · 集成 **106** 项（比上一版多 29 条，
  全是这次两处修复的断言）
- 真实 Edge 端到端 **29 个场景 / 312 项全绿**，**仓库源码与打包产物各跑一遍**
- 两处修复都做了**反向验证**：把旧实现注回去，对应断言精准变红
  （第一处红 10 条、第二处红 3 条），其余保持绿

## 已知事项

1.2.3 修的那个「一张卡片两份测验只做第一份」**还没在你的真实课程里验证过** ——
测试用的是照真实结构搭的模拟页。方便的话再跑一次那张卡片（两个大题目：
第六单元检测 / 章节测试），确认第二份被做掉。
