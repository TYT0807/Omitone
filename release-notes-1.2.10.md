# Omitone 1.2.10

## 新增了什么

**弹窗底部多了一个「复制日志」按钮。**

点一下，最近的运行日志会**整段复制成纯文本**，直接粘贴到聊天窗口、邮件或反馈里就行 ——
不用截长图，也不用在弹窗里手工选中（弹窗一失焦还可能被关掉）。

复制出来的内容长这样，开头带版本号和条数，方便对号：

```
Omitone 诊断日志（版本 1.2.10）
导出时间：16:58:20
共 42 条（新的在后）
---
[info] 16:57:01 study begin {"attachments":2}
[warn] 16:57:08 search job: every candidate frame was rejected {"frames":6,...}
```

## 为什么加这个

插件出问题时（比如**某个任务点怎么都不做**、**整章被跳过**），原因**只能从日志里看出来**。
上一版刚把这类问题的日志补详细了，但发现一个尴尬的事：**日志能看，却送不出来** ——
想让别人帮忙看，只能截 200 行长图。

这一版把最后一步补上：**一键复制，粘贴即发**。

## 对你有什么影响

- **插件的行为一个字没改**，日常使用感觉不到差别
- 只有"出问题要找人帮忙看"的时候，会方便很多

## 升级方式

下载 `omitone.zip` → 解压覆盖旧目录 → 在 `edge://extensions` 点一下重新加载，
然后**刷新学习通页面（F5）**。

> 说明书的「出问题先看日志」那一节也补上了这个按钮的用法。
