# Omitone 1.2.8

> 这一版把原本分开发的 1.2.8 / 1.2.9 / 1.2.10 / 1.2.11 **合并成一条** —— 它们本来就是同一轮修复，更新量很小。

## 修了什么

页面把「身份号」挂在**外层容器**上时，插件只能单向对上「任务点」和「页面上那块内容」，于是两种症状：

1. **认不出来**：任务点匹配不到 → 整章被静默跳过（一张卡片挂两份测验时，第二份连试都没试）。
2. **认出来了却不动手**：任务点匹配得上、视频却不动、测验不答，而且**还不报错**。

这一轮把两半都补齐，并配齐排查手段：

- **① 认不出**：日志现在会**逐帧说明卡在哪一条**（哪一帧、被哪条规则拒了），不再只有一句「没匹配上」。
- **② 复制日志**：弹窗底部加「复制日志」按钮，把整段日志一键复制成纯文本，粘贴就能发出来。
- **③ 认出来了却跑不动**：修掉「正向匹配成功、反向反查失败」的那一半 —— 任务点现在能真正跑起来（视频、测验都会动）。
- **④ 日志 + 复制日志** 让「整章被跳过 / 某个任务点不做」这类问题，第一次就能靠日志定位，不用再反推。

## 对你有什么影响

- 如果你遇到过「第二个测验不做」「视频放着不动」，这一版应该就好了。
- 装法、用法、界面、下载方式全都没变；设置、密钥、已记住的答案都不会丢。
- 仍建议跑一次真实课程页确认；若仍有异常，用弹窗「复制日志」把日志发来即可定位。

## 升级方式

下载 `omitone.zip` → 解压覆盖旧目录 → 在 `edge://extensions` 点一下「重新加载」。

> 只重载扩展、不刷新学习通页面，旧脚本会和新扩展断连，症状是「功能没反应」—— 刷新页面即可。
