# Omitone 1.2.8

**修一个会让「整章被跳过」的静默错** —— 症状与你之前报过的那个**完全一样**：
一张卡片里有两个测验任务点，第二个连试都没试，插件直接进了下一节。

## 这次修的是什么

插件要认出「这一页属于哪个任务点」，靠的是从页面结构里找它的身份号（jobid）。
页面结构有三层，jobid 挂在**外层**，题目在**最内层** —— 所以插件会沿着外层往上找。

问题出在「往上找」的那段代码里有一个**永远不成立的条件**：

```js
var holder = wrap.querySelector('[jobid], [data*="jobid"]');
var belongsToThisFrame = !!holder && (holder === wrap || ...);
//                              ^^^^^^^^^^^^^^^^^^ 永远不成立
```

`querySelector` 只会去找**里面的**元素，**永远返回不了它自己** ——
所以「jobid 写在任务点外层那个 div 上」这种情况**从来没被处理过**，
尽管代码注释里写着这在真实页面上**很常见**。

**后果**：找不到身份号 → 一个任务点都匹配不到 → 插件认为"本页没有任务点" →
**直接跳到下一节**。整章就这么过去了。

## 怎么确认它真的坏了（而不是我猜的）

这个错光读代码很难确信（代码**看起来**处理了这种情况），所以我按真实结构搭了一个
**变体页面**（jobid 只写在容器 div 上、iframe 上一个属性都不写）跑了一遍。
结果**毫不含糊**：

```
容器 div 上的 jobid: work-A / work-B     ← 确实在那儿
iframe 上的 jobid:   （空）              ← 确实是空的
查出来的结果:        （空）              ← 查不到
匹配到的任务点:      null                ← 一个都没匹配上
```

316 项检查里**只有这 2 项**变红，说明打得很准。修完之后 **317/317 全绿**，
而且原来那条「不能把隔壁任务点的身份当成自己的」的保护**仍然有效**
（认错任务点比认不出来严重得多：认不出来只是漏做，认错了是**做错**）。

## 对你有什么影响

- **如果你的课程里任务点一直是正常跑完的，那这一版你感觉不到变化**
- **如果你也遇到过"一张卡片里第二个测验不做、直接跳下一节"** —— 这一版很可能就是你要的
- 装法、用法、界面、下载方式**全都没变**；密钥、设置、已记住的答案都还在
- 升级方式：下载 `omitone.zip`，解压覆盖旧目录，然后在 `edge://extensions` 点一下重新加载

## 怎么确认没改坏

- `npm test` 全绿：自检 **18** 项 · 提示词基准 · 集成 **106** 项
- 真实 Edge 端到端 **30 个场景 / 317 项全绿**，**仓库源码与打包产物各跑一遍**
- 这一条的验证顺序与别处相反、但**更可信**：**先写测试、先看到它红**，再修，再看到全绿

## 已知事项

⚠️ **这一版仍然没有在真实平台上验证过。**

测试用的是照真实结构搭的模拟页面。现在覆盖了**两种** jobid 挂法
（写在 iframe 上 / 写在容器 div 上），但**真实页面到底用哪种、有没有第三种**，
只有你在真实课程里跑一次才能确认。

方便的话，请再跑一次那张卡片（两个大题目：第六单元检测 / 章节测试），
看第二份是否被做掉。**如果还是不做**，请把运行日志发我 —— 这一版新增的诊断会直接
指出它卡在哪一层。
